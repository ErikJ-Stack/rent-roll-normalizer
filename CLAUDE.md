# CLAUDE.md

> Onboarding doc for any Claude session (chat or Claude Code) working on this repo. Read this first — it points to canonical truth and surfaces facts that previously had to be grubbed for.

**Last updated:** 2026-05-28 (Track 4 — **UWT v0.7.0 ships: v6 template absorbed (T-12 Analysis income restructure) + substrate v0.2.15**. Operator authored the v6 template (Layer-3 INCOME rebuilt from the GPR→Net-Rent waterfall into a full actual-T12 build: Base Rent IL/AL/MC + LOC by care + ancillaries + contras → EGI at N77; GPR waterfall demoted to a DIAGNOSTIC sub-section N80-83; new **Auto Expense** non-labor row at N114). Claude Code absorbed it across 5 commits (`9a51905` registry → `bf3f61a` engine → `9014642` substrate → `3f8e5d0` formula-preservation → version/docs). **Row map verified cell-by-cell against `assets/ALF_UW_Template_v6.xlsx`** (not trusted from the handoff doc) — caught two template bugs. **Registry 0.4.2 → 0.5.0** (`_absorb_v6.py`): `templates.v6` (income_model=actual_t12), 70 retargeted T-12 Analysis concepts (income restructured; labor +14; non-labor +14 then +15 after Auto Expense; NOI +15; `t12_raw_month_*` 122→137), 14 new concepts (`base_rent_il/al/mc`, `total_base_rent`, `loc_il/al/mc`, `total_loc`, 5 ancillary revenue, `opex_auto_expense`); `second_person_revenue` gap_source→mapped (N66); `base_rent_normalized`+`loc_revenue` v6 targets **nulled** (N61/N65 are template SUM formulas — `loc_revenue` was clobbering N65). 137 concepts. **Engine**: `Auto Expense` added to `dashboard_model._LABELS_NON_LABOR` — it was routed by Description_Map but absent from the non-labor sum, so its $ fell out of opex → standardized EBITDARM overstated. **Verified = $6,061.32 on Homestead = the exact standardized-vs-as-reported NOI gap** ($1,417,385 vs $1,411,324). NOI now ties as-reported $1,411,324. 13 new keys exposed in `uw_output_model`. **Substrate v0.2.15** (`migrate_to_v0215.py`): 2nd-Person Description_Map re-map of r127 (Second Person Fee, included per operator decision) + r400/401/402 (Second Persons Revenue | care) `Base rent — *` → `2nd Person Revenue`. **Critical companion fix the handoff missed** (caught by verifying the EGI formula chain): `T12 Analytics!E52/F52` did NOT sum the "2nd Person Revenue" label, so the re-map alone would drop the 2nd-person $ ($32,220 on Homestead = r127) out of EGI → migration amends E52/F52 with an INDEX/MATCH term for that label; engine + dashboard EGI updated to match. EGI stays $7,001,957 (re-map neutral). **All 5 test suites green**; cached-fixture divergences whitelisted (5 keys × $6,061 Auto Expense + 2 keys × $32,220 re-map — rebuild fixtures against v0.2.15 to retire). v6 populate verified end-to-end: income rows + Auto Expense N114 + 2nd Person N66 populate; all total/subtotal formulas (N61/N65/N77/N99/N126/N131-133) preserved. **Default still v5 — two operator follow-ups before flipping to v6**: (1) v6 binary is the pre-Excel-resave version (39 zip parts, missing `xl/metadata.xml` + `xl/webextensions/`) → Section R/S spills degraded on outputs until operator opens v6 in Excel + saves + re-drops (`BUNDLED_UW_TEMPLATE_VERSION` stays `"v5"`); (2) v6.1 fix — `B56:M56` Layer-3 monthly headers still `=C122..=N122` but the raw row moved to 137 (+15), operator's repointing pass missed this chain (openpyxl quirk #4), cosmetic. **The preflight + verify-canonical-source discipline paid off repeatedly this session** — caught that RR v1.19.0 had already shipped (`5fc2a06`, redone from a stale OneDrive view before the catch), and caught the EGI-formula regression the v6 handoff's "EGI unchanged" note glossed over. `UWT_VERSION` 0.6.4 → 0.7.0. Earlier on 2026-05-28: Track 4 — **UWT v0.6.4 ships: Section I (Layer 1 — Raw T-12) populated**. Operator: "the summarized Raw data should be inserted in Section I." Section I = raw-T12 paste grid (rows 122–172: Acct#/Account Name/Apr-25..Mar-26/T-12 Total/→MAPPING), previously an empty skeleton. Decisions confirmed via AskUserQuestion: **rebuild one row per Analyzer label** + **Account Name = matched GL account names**. **Engine** `compute_t12_raw_lines(t12_result)` → `[{label, section, descriptions, monthly[12], total}]` grouped by Description_Map label from `T12ParseResult.gl_rows` (NOT read from Analyzer T12 Raw Data sheet — its monthly cells are formulas → blank on fresh Analyzer, cache caveat). **Writer** `_write_section_i_raw` + `populate_uw_template(..., raw_t12_lines=)`: clears skeleton (123–172, A–P), writes one row per label (B=matched GL names joined, C–N=12 months, O=total, P=bucket); authors Section J raw-totals reconciliation (Total Revenue/OpEx/EBITDAR) as live SUM formulas. **Caught a column-offset bug mid-build**: reused Layer-3 `_T12_MONTH_COLS` (B–M) but Section I months are C–N (col 3–14, since Acct#/Name occupy A/B) → first month overwrote Account Name; fixed to `range(3,15)`. **Result (Homestead)**: 44 raw lines (8 rev/36 exp), 663 cells; each row's months tie to its total; Section J EBITDAR = $1,411,324 (penny-exact as-reported NOI, vs standardized Layer-3 $1,417,385 — the ~$6K gap is bad-debt-as-revenue-contra standardization). New test `test_section_i_raw_populated`. `UWT_VERSION` 0.6.3 → 0.6.4. **All 3 operator requirements from the bug report now shipped** (totals as formulas, math ties, raw data in Section I). Earlier on 2026-05-28: Track 4 — **UWT v0.6.3 ships: T-12 Analysis totals as live formulas + income-waterfall sign fix**. Operator-reported math inconsistencies on populated Layer 3: totals were pasted values (didn't tie/recompute), the GPR→Net Rent waterfall didn't subtract losses ("$9.5M GPR minus losses ≠ $6.98M"), and N114 Total Op Ex = 0. Two operator decisions confirmed via AskUserQuestion: **bad debt reduces EGI** (honor template's N63 formula) + **monthly totals mirror formulas across B–M**. **Root cause**: writer pasted values over the template's live total formulas (N63/N69/N85/N111/N114/N115/N116), and pasted income contras with Analyzer-convention signs (Vacancy +, Bad Debt +) while the template's additive Net Rent formula treats them as reductions. **Fix** (`uw_template_writer.py`): (1) the 9 Layer-3 total concepts (`base_rent_normalized`/`egi`/`labor_total`/`opex_nonlabor_total`/`opex_total_incl_mgmt`/`opex_total_excl_mgmt`/`ebitdarm`/`ebitdar`/`ebitda`) are skipped on T-12 Analysis so values never overwrite formulas (fixes N114=0); (2) `_T12_CONTRA_KEYS` (`loss_to_lease`/`physical_vacancy_loss`/`bad_debt_writeoffs_revenue`) negated on write (annual + monthly) so the waterfall subtracts; (3) `_finalize_t12_layer3` preserves col-N formulas, **authors N117 EBITDAR=`=N116-N113` + N118 EBITDA=`=N117`** (template left them blank), and **mirrors each total's col-N formula across B–M**; Net Rent monthly (B63–M63) pasted as base−concessions−baddebt (GPR waterfall has no monthly). **Result (Homestead, evaluated)**: all 9 totals live formulas; Net Rent $6,750,961, EGI $6,964,627 (bad debt now revenue contra), Total Non-Labor $2,136,601 (bad debt out of opex); **EBITDARM $1,767,483 / EBITDAR $1,417,385 / EBITDA $1,417,385 UNCHANGED** (bad debt nets out of both EGI and opex at EBITDARM). Monthly ties to annual to the penny. Tests updated with an N-chain evaluator (openpyxl can't compute formulas). `UWT_VERSION` 0.6.2 → 0.6.3. **Still open: Section I (Layer 1 — Raw T-12, rows 121+) empty — next item.** Earlier on 2026-05-28: Track 4 — **UWT v0.6.2 ships: T-12 Analysis monthly grid populated (cols B–M)**. Operator-reported: populated UW Template shows the T-12 Total column but the 12 monthly columns (Apr 2025…Mar 2026) are all $0. This was the original Phase-2 "monthly grid annual-only" locked default (writer wrote col N, left B–M blank) — by design, not a regression — but the operator wants monthly trending visible. **Layout confirmed**: Layer-3 months = cols B–M (headers `=C122…=N122`), T-12 Total = col N, then O/P/Q (T-3 Annlzd / Per Bed/Mo / % of EGI); the B–M cells are literal-`0` paste targets (NOT formulas, even on subtotal rows like EGI N69 which IS a formula but B69–M69 are plain 0s), so each month must be pasted explicitly. **Engine**: new `compute_uw_output_monthly(rr_result, t12_result) → {concept_key: [12 floats]}` reusing `_aggregate_t12`'s month-by-month GL bucketing (same primitive as the annual evaluator + `dashboard_model`); covers all T12-derived concepts (labor, non-labor opex, base rent/LOC/other-rev, subtotals, EBITDA chain). GPR/physical_vacancy_loss/loss_to_lease OMITTED — RR projections with no monthly source (rows stay blank, matching T12 Analytics). **Writer**: `populate_uw_template(..., computed_monthly=None)` + `_write_monthly_grid`; for any concept targeting `T-12 Analysis` col-N (annual), pastes 12 values across cols B–M of that row; written unconditionally (pure paste target, no cached source); new `summary['monthly_cells_written']`. **App**: passes `compute_uw_output_monthly(...)`; success caption notes monthly cell count. **Verified**: 636 cells (53 concepts × 12); every populated row reconciles to the penny — Base Rent ΣB:M=$6,983,357=N, EGI=$7,001,957=N, Care Staff Labor=$1,184,692=N, Total Labor=$3,060,543=N; GPR row blank monthly (N=$9,524,893) as designed. New test `test_monthly_grid_reconciles`; all 4 model tests + writer suite green. `UWT_VERSION` 0.6.1 → 0.6.2. **Not in scope**: cols O (T-3 Annlzd), P (Per Bed/Mo), Q (% of EGI) stay blank — per-row denominator/sign semantics unconfirmed, never populated before (no regression); follow-up candidate. Earlier on 2026-05-28: Track 4 — **UWT v0.6.1 ships: dynamic-array repair — Section R/S spills survive the writer**. Operator-reported bug on a populated UW Template: Section R (Unit Type Pricing By Care Level, RR Analysis rows 170–181) shows only ONE row (AL · 1 Bedroom) instead of the full Care×UnitType matrix; row 180/181 totals understated (~10×). No #SPILL!/#VALUE! — silently wrong. **Root cause = openpyxl quirk #6 hitting the OUTPUT**: `wb.save()` drops `xl/metadata.xml` (XLDAPR) + the per-cell `cm="1"` markers, so the template's `Z173=SORT(UNIQUE(FILTER(...)))` driver + `A173:Q173` spill demote to single-cell legacy CSE arrays → return only the top-left value. An Excel re-save does NOT recover it (Excel commits to the CSE interpretation on open) — the old "open in Excel + save" workaround was insufficient for dynamic arrays. **Fix**: new `_restore_dynamic_arrays(output_bytes, template_bytes)` in `uw_template_writer.py` — pure `zipfile`+`re` post-processor (no new dep) called after `wb.save()`. Re-injects `xl/metadata.xml` verbatim from the template, adds its content-type Override + workbook `sheetMetadata` relationship, and re-applies the `cm` markers to the exact anchor cells that had them (matched by sheet name — robust to openpyxl rel-id renumbering + `/xl/…` absolute targets; only cells still holding a formula). Faithful by construction (writer never edits the anchor cells). No-op when template has no metadata.xml (v4); try/except so a repair failure degrades to a warning. **Verified**: 557 cm markers restored (554 RR Analysis + 3), Z173/A173/C173/Q173 all carry `cm="1"`, metadata.xml present, zip valid, openpyxl re-loads, writer data intact (EGI $7,001,956.79 @ N69, D211='1 Bedroom'). New test `test_dynamic_array_metadata_restored`; both UWT suites green. **A subtle bug caught mid-fix**: openpyxl writes rel targets as `/xl/worksheets/sheetN.xml` (absolute) vs the template's relative `worksheets/sheetN.xml`; my first path-normalization produced `xl/xl/...` (double prefix) → 0 cm injected. Fixed to handle both forms. `UWT_VERSION` 0.6.0 → 0.6.1. **openpyxl quirk #6 update**: the UW Template writer now AUTO-REPAIRS dynamic arrays on output — the manual Excel-resave workaround is retired for this path. **Secondary issue flagged (not fixed)**: 128/400 `X211:X610` rows resolve to "" (occupied-filter); worth a sanity check that no occupied unit is missing Care Level/Unit Type. Earlier on 2026-05-28: Track 4 — **UWT v0.6.0 ships: in-Python UW Output evaluator — the cache caveat is CLOSED**. The biggest UX friction in the populate flow is gone: the writer no longer needs the Analyzer to have been round-tripped through Excel. New module **`uw_output_model.py`** (`compute_uw_output_values(rr_result, t12_result, *, scenario) → {concept_key: value}`) computes the 62 `uw_output`-system concepts + 2 dependent `derived` (`licensed_beds_total`, `opex_total_incl_mgmt`) directly from the parsed RR + T12, reusing Track 5 `dashboard_model`'s aggregation primitives (`load_description_map`, `_aggregate_t12`, `_LABELS_*`). **Why it's correct:** `UW Output` is a thin reference layer over `T12 Analytics` (sum `T12 Raw Data` per Description_Map Label + `Rent Roll Recon` bed counts). The default **normalized** scenario equals T12 actual for every line because opex/other-rev are `F{r}==E{r}` (col F literally copies E) and base rent/LOC's stabilized formula `B20=B6·B10·B19·12` collapses to the actual when `B10` (target occ) `=B8` (its literal default formula). Verified empirically on Homestead: `E16==E20`, `E23==E27`, `E52==F52`, `E108==F108`. **Writer:** `populate_uw_template(..., computed_values=None)` — fallback applied **per concept only when the Analyzer's cached cell read is blank**, so an analyst-saved override Analyzer still wins; new `ConceptResult.computed_fallback` flag + `summary['computed_in_python']` count; fully backward-compatible (existing smoke test still passes, `computed_in_python: 0` on the cached fixture). **App:** populate flow calls the evaluator + injects `property_name` (Prop Info!B4) and `rr_period_date` (RR Analysis!B5) — both already in hand, both blank on a fresh build; the loud cache-caveat `st.warning` replaced by an `st.success` ("N values computed in-Python … no Excel round-trip required") + soft `st.info` for the genuine no-T12 case. **Verification (`tests/test_uw_output_model.py`, new):** engine reproduces Homestead's cached `UW Output` to the penny on 42 concepts (EGI $7,001,956.79, EBITDARM $1,767,482.75, EBITDA $1,417,384.90, GPR $9,524,893.30); writer-fallback e2e on a freshly-built (no-cache) Analyzer takes T-12-path `no_source` from **63 → 2** (61 computed in-Python), and `T-12 Analysis!N69/N116/N118/N115` all carry correct values. No new dependency (pure Python). `UWT_VERSION` 0.5.3 → 0.6.0; registry unchanged at v0.4.2 (writer/app addition, no registry change). The 2 residual fresh-Analyzer no_source are closed by the property/period injection; the 7 `rent_roll`-path no_source (pharmacy/meal/scooter/care-level/preleased) are legitimately empty source columns, not cache artifacts. Earlier on 2026-05-27: Track 1 — **RR v1.19.0 ships: River Oaks / SSMG-Yardi format support + Deposit capture** (committed `5fc2a06`). New operator fixture (River Oaks, Senior Solutions / Yardi `.xls`) surfaced an unhandled rent-roll shape + the first Required Deposit column — closing the v1.18.1 / substrate v0.2.14 carry-forward that reserved `Rent Roll Input!AI`. `requirements.txt` +`xlrd>=2.0.1` (pandas content-sniffs OLE2 → routes `.xls` to xlrd); `app.py` 3 file_uploaders accept `.xls`; `mappings.py` Admin/Down→Vacant, RHA→IL, MEDADMIN/MEDMANAGE→Med Mgmt $; `normalizer.py` Yardi header patterns + new `deposit` field + care-type fallback 4.5 (re-run on raw apt_type) + `CONDENSED_COLUMNS` col 32 Deposit; `analyzer_rr_writer.py` Deposit→AI (`$#,##0.00`). River Oaks 89 rows→86 beds; Homestead unchanged 176 (IL 62/AL 62/MC 52). Earlier on 2026-05-27: Track 4 — **UWT v0.5.3 ships: v5.1 K/L/V template-formula absorption**. Operator dropped a corrected `ALF_UW_Template_v5.1.xlsx` 2026-05-27 ("the new updated one with corrections in the rent roll analysis tab"). Six new template formulas at Rent Roll Analysis row 211+ (fill-down through 609): K211 `=IFERROR(IF(A211="","",N(AE211)+N(AF211)+N(AG211)+N(AH211)),0)` Total LOC from per-fee ancillaries · L211 `=IFERROR(IF(A211="","",N(J211)+N(K211)),0)` Total Sched (J+K) · V211 `=IFERROR(IF(OR(...),"",J211/U211),"")` Actual PSF per month · W211 `=V211*12` Actual PSF per year · AA211 `=I211-J211` Mkt-Actual $ delta · AB211 `=AA211/I211` Mkt-Actual % delta. W/AA/AB were already classified `derived` in the registry's `intake_targets_unmapped` block (no concept mapped). **K/L/V were not — three concepts had been writer-paste-targeting them**, so a collision sweep was needed. `tools/uw_template/_absorb_v51_total_formulas.py` reclassified `rr_total_loc` (K211+), `rr_total_monthly_rev` (L211+), `rr_actual_psf` (V211+) from `mapped → derived` so the writer skips and the template formulas self-derive at populate-time. Precedent: matches `rr_total_ancillary` (became `derived` in v0.4.0 when v5 added `=SUM(AK:AO)` at AQ). **Registry 0.4.1 → 0.4.2**. Status rollup: 104 mapped (was 107) / 4 proposed / 6 derived (was 3) / 5 gap_source / 2 gap_target / 1 header_only / 1 substrate_ready_parser_pending. Filename consolidated: operator's `v5.1.xlsx` renamed to `v5.xlsx` per established v0.5.1 policy (minor revisions reuse the v5.xlsx filename; version lives in `templates.{v}` block). Operator's v5.1 file has all 46 zip parts intact including `xl/metadata.xml` (XLDAPR/fDynamic) and `xl/webextensions/` (Claude-for-Excel) — healthier than prior v5 in HEAD (39 parts after past openpyxl ops). **Smoke test passes**: empty Analyzer 2 cells / 15 skipped / 106 no_source; Homestead populated 99 concepts / 2,311 cells (was 102 / 3,244 in v0.5.2 — expected -3 row-stride × 176 rows + paste-source column adjustments). All 10 v5.1 template formulas verified intact in output (K, L, V, W, X, Y, AA, AB, AP, AT); writer-paste data intact alongside formulas (A211='A1', D211='1 Bedroom', E211='Occupied', J211=$2,926.84, U211=461 sqft, AR211='X' for ACH). T-12 spot-checks all green: EGI $7,001,957 at N69, EBITDA $1,417,385 at N118, EBITDARM $1,767,483 at N116, GPR $9,524,893 at N58. **Output xlsx caveat (openpyxl quirk #6)**: the writer's `openpyxl.save()` still drops `xl/metadata.xml` + `xl/webextensions/` on the populated UW Template output (39 parts vs input's 46). Operator workaround unchanged from v0.4.3+: open populated file once in Excel + re-save → Excel rebuilds metadata from the dynamic-array formula calls. In-Python formula evaluator remains queued as the structural fix. **Carry-forwards bundled into [`2026-05-27-uwt-v51-template-formulas-K-L-V.md`](tools/uw_template/handoffs/2026-05-27-uwt-v51-template-formulas-K-L-V.md) (Verified)**: (a) A173/B173 IFERROR wrapper stripped in operator's v5.1 (authored from pre-v0.4.4 baseline) — when Z173 spill empty, A173/B173 throw `#N/A`; recommend re-wrap in next Cowork pass; (b) Cover G1/H1 substrate stamp still empty (from 2026-05-26 brief, still Pending); (c) Rent Roll Analysis B5 date cell still empty (from 2026-05-26 brief, still Pending). All three bundleable into a single next-v5.1-revision author pass. **The 2026-05-26 preflight ritual proved its value this session** — fresh `git ls-files` + registry version + handoff inventory + `UWT_VERSION` check caught that significant work had shipped between the previous chat and this one (v0.4.4 / v0.5.1 / v0.5.2), so the v5 → v5.1 reconciliation work started against actual current state rather than memory of last session. Earlier on 2026-05-27: Track 4 — **UWT v0.5.2 ships: T-12 monthly headers + cache caveat surfaced**. Operator-reported after sample-running v0.5.1: "T-12 data isn't populated. Check that the headers match from the analyzer. Header dates should be actual months and Year that's in the T-12 raw data." Three findings on investigation: (1) labels DO match correctly — registry's target rows align with T-12 Analysis labels (EGI→N69, EBITDARM→N116, etc., no fix needed); (2) T-12 data not populated = **cache caveat in action** — the Analyzer the writer reads has formula text but no cached values (openpyxl-doesn't-compute-formulas), so writer's `_resolve_source()` returned None for every t12-path concept, producing `no_source` outcomes silently; template's fallback SUM formulas at N69/N85/N111/N116 evaluate to 0 because upstream cells are all empty; (3) hardcoded month headers — `T-12 Analysis!C122:N122` are static `Apr-25..Mar-26` strings, wrong for any other T-12 period. **Fix #1** (`tools/uw_template/_phase4_add_month_headers.py`): 12 new scalar concepts `t12_raw_month_1..12` under new path `t12_raw`, source `cell` system reading T12 Input!{C..N}11 (operator's actual T-12 month labels like 'Apr 2025'..'Mar 2026' populated by T12 normalizer), target T-12 Analysis!{C..N}122. Auto-cascades to row 56's monthly headers via existing `=C122..=N122` formula chain. Registry 0.4.0 → 0.4.1. Total concepts 111 → 123. New category `t12_raw_headers`. **Fix #2** (cache caveat UX): promoted in-app banner from `st.info` to `st.warning` with explicit 4-step workaround (download Analyzer → open in Excel → save → upload as Analyzer override → re-download UW Template); added ⚠️ Cache caveat callout to Workspace "What the app does" expander so operators see it BEFORE hitting the issue. UWT 0.5.1 → 0.5.2. **Writer regression**: 102 concepts written / 3,244 cells (was 90/3,232 — +12 month headers); spot-check C122='Apr 2025'..N122='Mar 2026' (real T-12 months). Test concept-count assertion updated 111 → 123. Empty-Analyzer smoke + Homestead e2e both pass. **In-Python formula evaluator (BIG follow-up)** logged as roadmap item — embed `formulas`/`pycel`/`xlcalculator` (or extend `dashboard_model.py` pure-Python pattern from Track 5) so writer reads computed UW Output values directly from in-memory Analyzer without requiring Excel round-trip. Estimated 4-8 hours; substantial but eliminates the biggest UX friction in the populate flow. Earlier on 2026-05-27: Track 4 — **UWT v0.5.1 ships: v5.1 column restructure absorbed**. Operator authored v5.1 in Excel per the 2026-05-27 column-restructure handoff brief: Unit Type inserted at new Rent Roll Analysis col D (before Status), old W "Unit Type (base)" + old AC "Apt Type" dropped, old D-V cols right-shift by 1 to E-W, old AD-AV cols left-shift by 1 to AC-AU (closing the AC hole), net col count 48 → 47. Operator saved as `assets/ALF_UW_Template_v5.1.xlsx`; consolidated to single canonical filename by replacing `assets/ALF_UW_Template_v5.xlsx` with patched v5.1 content (old v5 preserved in git history). Re-applied v0.4.4's A173/B173 IFERROR wrappers via openpyxl since operator's v5.1 was authored from a v5 snapshot pre-dating that fix. Registry absorbed via `tools/uw_template/_absorb_v51_column_restructure.py`: 36 rent_roll concept v5 targets shifted (18 right-shift D-V→E-W; 17 left-shift AD-AV→AC-AU; `rr_apt_type` special-retargets AC→D). **CRITICAL absorber-bug found mid-release**: initial implementation shifted ALL 94 concepts including T-12 Analysis ones (`egi: N69 → O69`, wrong since T-12 Analysis cols didn't change); fixed to filter on `target.sheet == "Rent Roll Analysis"`, reverted registry via `git checkout HEAD --`, re-ran cleanly. `registry_version` 0.3.0 → 0.4.0. UWT_VERSION 0.4.4 → 0.5.1 (skipping v0.5.0 used by rolled-back metadata-cells attempt). **Writer regression on Homestead populated fixture passes**: 90 concepts written / 3,232 cells (identical totals to v5); D211='1 Bedroom' (new Unit Type col, writer paste from Analyzer col F); E211='Occupied' (Status, right-shifted from D); U211+ = Sq Ft writer paste (was T); AP211='=SUM(AJ211:AN211)' (Total Ancillary template formula, left-shifted from AQ); AR211='X' (ACH, left-shifted from AS). T-12 Analysis cells UNCHANGED at original N69/N115/N116/N118 positions. `tests/test_uw_template_writer.py` e2e test updated for v5.1 layout — the stale `AQ211 should hold =SUM(...)` v5-era assertion replaced with broader v5.1 layout spot-checks. `_absorb_v51_column_restructure.py` retained as audit trail. v5.1 metadata cells (Cover stamp + RR Period Date) NOT included in this release — operator may add in subsequent Excel pass; `_absorb_v51_metadata_cells.py` still pre-wired. **Openpyxl quirk #6**: operator did column restructure in Excel directly (not via openpyxl) per the rolled-back-v0.5.0 lesson. My A173/B173 IFERROR re-application via openpyxl drops `xl/metadata.xml`; operator opens file in Excel once + accepts repair + saves to rebuild — working pattern since v0.4.3. Earlier on 2026-05-27: Track 4 — **UWT v0.4.4 ships: Section R re-fix on v5 template**. Operator-reported bug: Section R on Rent Roll Analysis returns #N/A and zeros despite v0.4.3's W/X/Y fill-down patch. Root cause: operator's 2026-05-26 `deacc41 chore: refresh assets/ALF_UW_Template_v5.xlsx with operator edits` replaced v0.4.3's `W = =AC{r}` formulas with a substring-Notes-parser (an option I'd merely sketched in earlier chat, never recommended — operator authored it into the template during their Excel pass). Since real rent rolls don't carry "studio" / "1 bed" / "1br" wording in Notes col S (it's free-form lease/concession context), W resolved to "" everywhere → X "" everywhere → Z173 dynamic-array SORT/UNIQUE/FILTER spill empty → all of A173-I173 broken (#N/A on A173/B173 TEXTBEFORE/TEXTAFTER, zeros on C173-G173 COUNTIFS/MINIFS/AVERAGEIFS/MAXIFS, etc). **Fix** via `tools/uw_template/_patch_v5_section_r_use_ac.py` — 4 surface changes to `assets/ALF_UW_Template_v5.xlsx`: (a) `W211:W610` replaced with `=IF(AND($D{r}="Occupied",$AC{r}<>""),$AC{r},"")` — gated AC reference (col AC holds canonical Apt Type already populated by writer from Analyzer col F normalized closed vocab); (b) `A173` wrapped in IFERROR: `=IFERROR(TEXTBEFORE(ANCHORARRAY(Z173),"|"),"")` so empty spill returns blank not #N/A; (c) `B173` same: `=IFERROR(TEXTAFTER(ANCHORARRAY(Z173),"|"),"")`; (d) `D173` swapped from hardcoded `XLOOKUP(B173#,{"Studio";"1 Bedroom";...},{450;700;1000;1300;350;900},"")` (placeholder estimates) to `=IFERROR(AVERAGEIFS($T$211:$T$610,$C$211:$C$610,ANCHORARRAY(A173),$AC$211:$AC$610,ANCHORARRAY(B173)),"")` — real per-unit sq ft from col T averaged by Care|UnitType bucket. Patch idempotent (gate: W211 contains `$AC211<>""`); 9/9 verification checks pass; writer regression on Homestead still passes (90 / 3232 / W211 in output = new gated AC formula). UWT_VERSION 0.4.3 → 0.4.4. UWT_LAST_UPDATED 2026-05-26 → 2026-05-27. **Openpyxl quirk #6 implication**: this patch round-trips the file through openpyxl which silently drops `xl/metadata.xml` (XLDAPR dynamic-array block); operator opens in Excel once + saves → Excel detects + repairs the missing part → file works. Working pattern since v0.4.3; Excel's repair forgiving for surface-only formula changes. Section S (rows 184-188) was unaffected — its formulas reference col AU (Concession source tag) directly, no dependency on W/X/Z chain. Earlier on 2026-05-26: Track 4 — **UWT v0.5.0 attempted, rolled back same session** + **Handoff infrastructure ships and stands**. The v0.5.0 attempt: tried to close the two v5 → v5.1 `gap_target` concepts (`substrate_version`, Rent Roll Analysis tab-header Period Date) via direct openpyxl edits to `assets/ALF_UW_Template_v5.xlsx` — `Cover!G1/H1` substrate stamp + `Rent Roll Analysis!B5` mm/dd/yyyy date cell. Cell-level fidelity diff at Worksheet-object level appeared clean (16 sheets, 240 merged ranges, 5 defined names, 3417 RRA cells, Section R/S ArrayFormulas verbatim). **Rolled back after discovering xlsx-zip-part inventory diff showed openpyxl silently dropped 6 parts on save**: critically `xl/metadata.xml` (810 bytes — `XLDAPR` / `fDynamic="1"` block holding Excel's dynamic-array properties; without it the v0.4.3 Section R/S spilled-range formulas `Z173 SORT(UNIQUE(FILTER(...)))` / `C173 COUNTIFS(...,ANCHORARRAY(Z173))` would likely demote to single-cell or `#SPILL!` in Excel), `xl/webextensions/*` (Claude-for-Excel taskpane add-in `wa200009404`), `xl/sharedStrings.xml` (74 KB — inlined instead, functionally equivalent), `xl/calcChain.xml` (167 KB — Excel rebuilds on open), plus minor comment/VML rename trims. openpyxl has no API to preserve `metadata.xml` or `webextensions/` on save. **Rollback executed**: template restored from git `deacc41`; registry reverted v0.3.1 → v0.3.0 via `_revert_registry_to_v030.py` (substrate_version mapped→gap_target, rr_period_date mapped→proposed, t12_period_date derived_in_template→gap_target); 3 open_questions re-opened; UWT_VERSION restored to 0.4.3; artifacts regenerated. The two patch scripts (`_patch_v5_to_v51_metadata_cells.py`, `_revert_registry_to_v030.py`) retained as audit trail in `tools/uw_template/` — **do NOT re-run the patch script** without first solving the XLDAPR-loss problem. **Lesson recorded as openpyxl quirk #6**: openpyxl's `wb.save()` does not preserve `xl/metadata.xml` (dynamic-array `XLDAPR`/`fDynamic` props) or `xl/webextensions/` (Office add-in taskpanes). For any workbook using Excel 365 dynamic arrays — `SORT`/`UNIQUE`/`FILTER`/`ANCHORARRAY` with spill — a Python round-trip will silently break dynamic-array semantics even when every Cell object inspects clean. Cell-level fidelity diffs are necessary but not sufficient; also diff zip part inventory before declaring a round-trip safe. **Handoff infrastructure was already in place** from commit `031e24f` (earlier 2026-05-26 morning, ahead of this session): `tools/uw_template/HANDOFF_TRACKER.md` (running index, newest at top), `HANDOFF_TEMPLATE.md` (copy-and-fill blank), `handoffs/` directory with the original 2026-05-25 v4→v5 brief. **This session's contribution to the infrastructure was augmentative, not creative**: added `2026-05-26-uwt-v5-to-v51-residual-gaps.md` as the new active brief; marked the older 2026-05-25 brief Superseded with a banner explaining v5 absorption shipped via UWT v0.4.0 hours before that brief was published; added `Superseded` to the tracker's status legend; updated the tracker index with the newer row. Pattern (unchanged from 031e24f): Track 4 work that surfaces a template-side change produces a handoff brief; operator authors externally in Excel via Cowork; next chat absorbs registry-side. CLAUDE.md Track 4 section gained a "Handoff protocol" paragraph; user-level feedback memory written (`uw-template-handoff-protocol`). **Net delta vs start-of-session**: handoff infrastructure exists (new); v5 template binary unchanged; registry unchanged at v0.3.0; UWT version unchanged at 0.4.3; two `gap_target` concepts still pending (deferred to operator-authored v5.1 per the active handoff brief). **Surviving `open_questions` (8)**: Bad Debt placement, 2nd Person Revenue source, monthly grid B-M, A5/B5 date format, RRA rows 1-209 derived framing, AR aging row-level routing, Cover substrate stamp deferred to v5.1, RR Analysis tab-header Period Date metadata cell. Earlier on 2026-05-26: Track 4 — **UWT v0.4.3 ships: v5 template Section R/S formula fill-downs**. Operator-reported bug after v0.4.2 ship: Section R (Unit Type Pricing by Care Level, rows 170-181) returns #CALC! everywhere on populated UW Template. Root cause: dynamic-array driver at `Rent Roll Analysis!Z173` (`=SORT(UNIQUE(FILTER($X$211:$X$610,$X$211:$X$610<>"")))`) chokes on empty X column — FILTER on all-empty range with no if_empty arg throws #CALC!, poisons all of C173:Q179. Section S has same shape (counts 0 from empty AU column). Verified the Section R/S ArrayFormulas at row 173 are intact (Z173, C173, Q173 are `<openpyxl.worksheet.formula.ArrayFormula>` objects); the v5 template author cleaned data rows but never filled W/X/Y formula-derived columns. **Fix shipped** via `tools/uw_template/_patch_v5_section_r_formulas.py` — CLI-runnable, idempotent. Fills 1,200 cells (400 rows × 3 cols) at rows 211-610: **W = `=AC{r}`** (mirror AC, which holds Analyzer col F's normalized Apt Type already paste-populated by writer); **X = `=IF(AND(D{r}="Occupied",C{r}<>"",W{r}<>""),C{r}&"|"&W{r},"")`** (Care|UnitType, occupied-only — drives Z173 unique-key); **Y = `=IF(AND(C{r}<>"",W{r}<>""),C{r}&"|"&W{r},"")`** (Care|Unit, all care+type rows — drives Section R denominators). Patch verified end-to-end: 6/6 verification checks pass (headers preserved at W210/X210/Y210, W/X/Y at start/mid/end-of-range are formulas, W611 empty so no overwrite); idempotent on re-run; writer round-trip on Homestead fixture preserves W/X/Y formulas through openpyxl load+save (Z173 still ArrayFormula object); 176 AC writer-paste cells + 400 W formula cells. **AU left untouched** per contract §11 "Manual analyst entry" and operator's hedge ("rough sketch — confirm column meanings before using"); Section S will continue to show 0 counts until analyst-typed Conc Source data lands. Z (_key) / AA (Mkt-Actual $) / AB (Mkt-Actual %) also blank per contract §13 formula-derived list but operator hasn't reported them blocking any section — left alone for now. UWT_VERSION bumped 0.4.2 → 0.4.3. Operator-side note: patch operates on committed `assets/` asset; operator should re-run on their Deals folder canonical copy too (or replace it with the patched repo copy). Future v5.1 template work should preserve the W/X/Y fill-downs. Earlier on 2026-05-26: Track 4 — **UWT v0.4.2 Phase 2.5 follow-up ships: bundled UW Template + override pattern**. User feedback after v0.4.1: "I don't see a download for ALF UW Template" — because the v0.4.1 pattern required uploading a template every session. v0.4.2 mirrors the Analyzer's load pattern exactly. New constants `BUNDLED_UW_TEMPLATE_PATH = Path(__file__).parent / "assets" / "ALF_UW_Template_v5.xlsx"` + `BUNDLED_UW_TEMPLATE_VERSION = "v5"`. New helper `_load_uw_template(uploaded_file) → (bytes, source_label, version)` mirrors `_load_analyzer()` exactly: upload wins, falls back to committed asset, raises FileNotFoundError otherwise. New helper `_detect_uw_template_version(template_bytes)` probes `Rent Roll Analysis!AP210` ("Care Level Tier" — v5-only header) to auto-distinguish v4 vs v5; uses `read_only=True` for speed; falls back to v5 on any error. **Sidebar restructure**: removed standalone "UW Template (.xlsx) — optional" file_uploader; promoted scenario radio to always-visible (was conditional on upload); added "UW Template override (.xlsx)" to Advanced expander parallel to Analyzer override; version footer extended with `UWT v{UWT_VERSION}`. **Workspace populate flow restructure**: removed `if uw_template_file is not None:` guard so populate fires on every can_download; calls `_load_uw_template()` up front; new "Using UW Template: **<source>** (`<version>`)" caption mirrors existing "Using Analyzer: ..." pattern; passes resolved `template_version` to writer. UWT_VERSION bumped 0.4.1 → 0.4.2. app.py 1,456 → 1,523 lines (+67 LOC). Writer module unchanged; writer regression tests still pass on Homestead. UX impact: operator's `RR upload → both Analyzer + populated UW Template appear automatically as downloads`; override path under Advanced for legacy v4 / future v5.1 candidate. Earlier on 2026-05-26: Track 4 — **UWT v0.4.1 Phase 2.5 ships: Streamlit UI integration**. Closes the user-experience gap that was open since Phase 2: operator no longer has to run `python uw_template_writer.py` via CLI to get a populated UW Template. New sidebar uploader `"UW Template (.xlsx) — optional"` after the AR block + horizontal scenario radio (`normalized` default / `t12_actual`, rendered only when a template is uploaded). After the existing combined-Analyzer download button, the Workspace tab now shows a `📋 Populate UW Template` section (gated on `uw_template_file is not None`): reads template bytes → calls `populate_uw_template(final_bytes, uw_template_bytes, scenario=...)` over the just-built Analyzer → builds per-deal filename via `derive_property_name(rr_file.name)` (sanitized, format `<Property>_UW_Template_<RR period>_<scenario>.xlsx`) → surfaces 1-line summary caption ("Writer populated N of M concepts, X cells. Scenario: ...") + drill-in expander auto-expanded on warnings showing outcome counts + per-error notes → emits the second download button. **Cache-caveat info banner** conditional on any t12-path `no_source` outcomes — walks the analyst through the round-trip-through-Excel workaround since openpyxl doesn't compute formulas (the Analyzer bytes the app just built via `populate_rr_input`/`populate_t12_input`/`populate_ar_collections` have no cached UW Output values; analyst needs to download Analyzer → open in Excel → save → re-upload via Advanced expander's "Analyzer template override" → re-trigger the populate flow for fully-populated t12-path cells). New constants: `UWT_VERSION = "0.4.1"`, `UWT_LAST_UPDATED = "2026-05-26"`. `RR_VERSION` bumped `"1.18.0"` → `"1.18.1"` (constant was lagging behind the v1.18.1 writer shipped earlier the same day). New import block `from uw_template_writer import (UWTemplateWriterError, populate_uw_template)`. Error handling: catches `UWTemplateWriterError` for clean reporting; generic `Exception` fallback for unanticipated failures (matches pattern of existing RR/T12/AR error handlers). app.py parses clean at 1,394 lines; writer regression test still passes (90 concepts / 3,232 cells on Homestead v5). **Known limitation surfaced**: openpyxl-doesn't-compute-formulas means freshly-built Analyzer bytes from the app have no cached values for UW Output — t12-path cells come through `no_source`. The info banner surfaces the workaround clearly; future enhancement could embed pycel/formulas/xlcalculator for in-Python evaluation. No new tests for Streamlit wiring (existing writer-module tests cover the call path). Operator-side parallel work still pending: v5.1 template (Cover substrate stamp + RR Analysis tab-header Period Date cell); Deposit parser (needs source fixture); AR row-level routing. Earlier on 2026-05-26: Track 4 — **UWT v0.4.0 Phase 3 ships: UW Template v5 absorbed**. Operator authored `ALF_UW_Template_v5.xlsx` externally in Excel per the 2026-05-25 v4→v5 handoff brief; dropped at `Deals/Acquisition/_Template/ALF Templates/ALF_UW_Template_v5.xlsx` 2026-05-26 and copied to `Sample Files/ALF_UW_Template_v5.xlsx` as the registry's canonical copy. v5 is now the binding default; v4 retained for backward compat. **Registry → v0.3.0**: new `templates.v5` block (capacity 400 units, paste anchor `Rent Roll Analysis!A211`, monthly headers formula-driven from on-sheet Layer 1 row 122, sheet count 16 — `Additional Fees` removed). Per-concept `targets.v5` for every concept (inherit v4 unless v5 shifted/added). **7 gap_targets closed by v5**: `opex_total_excl_mgmt` → N115 (template formula `=N114-N113`); `ebitda` → N118; `occupied_beds_il/al/mc` → Prop Info B20/B21/B22; `rr_care_level_tier_label` → AP211+ (closes Analyzer col K gap); `rr_preleased_date` → AR211+ (closes substrate v0.2.14's AJ relocation). **1 moved gap_target → derived**: `rr_total_ancillary` → AQ211+ but template owns `=SUM(AK:AO)` formula; writer skips. **2 t12-path row shifts**: `ebitdarm` N115→N116, `ebitdar` N116→N117. **2 rr-path col shifts** per contract §16: `rr_ach` AP→AS, `rr_market_psf` AQ→AT (analyst-input cols pushed right by 3 to make room for new AP/AQ/AR). **Writer**: default `template_version` flipped v4→v5; reads `rent_roll_data_end_row` per template version (v4: 386, v5: 610) to cap the stride; CLI default also flipped. **Tests**: both smoke tests now exercise v5 by default; e2e spot-checks the 4 new v5 features (N115/N116/N118/Occupied Beds rows) + AQ-formula-preservation assertion (drift guard against the col-shift bug caught and fixed mid-release: `rr_market_psf`/`rr_ach` had inherited v4 targets blindly into v5 and were overwriting the template's formula). **Status rollup**: 92 mapped / 5 gap_target / 5 gap_source / 4 proposed / 5 other = 111 total (was 88 mapped / 10 gap_target). **Populated-Analyzer e2e on Homestead 176-bed fixture**: 90 concepts written / 3232 cells; EGI $7,001,957 unchanged, new EBITDA $1,417,385 at N118, new Total OpEx excl mgmt $5,234,474 at N115, Occupied Beds 53/40/35 at Prop Info!B20-B22; AQ211 formula `=SUM(AK211:AO211)` preserved. **Open questions: 11 → 8** (7 closed, 2 added — Cover substrate stamp deferred to v5.1, tab-header Period Date metadata cell still pending). Earlier on 2026-05-25 (same session): UWT v0.3.0 Phase 2 — writer module ships. Earlier on 2026-05-25 (Track 4 — UWT v0.2.1 / RR v1.18.1 / substrate v0.2.14): Cross-track follow-up resolves AI-column conflict. New `uw_template_writer.py` (~400 LOC) exposes a pure function `populate_uw_template(analyzer_bytes, template_bytes, *, scenario='normalized', template_version='v4') → (bytes, PopulateReport)` that drives off `tools/uw_template/registry.json`. Source-system dispatch on `concept.source.system`: `uw_output` (reads `UW Output!{F or E}{row}` per scenario; `B`/`C`/`D` for IL/AL/MC splits), `rr_input` (column-stride read of `Rent Roll Input!{col}7:606`), `named_range` (resolves to qualified address), `cell` (direct), `derived` (small hard-coded compute set — `licensed_beds_total`, `opex_total_incl_mgmt`). Target writes are scalar (single cell) or row-stride (address ends in `+`, writes down column from start row, blank source rows skipped). Loads Analyzer with `data_only=True` (reads cached formula values — Analyzer must have been saved through Excel) and template with `data_only=False` (preserves all template formula cells the writer doesn't touch). **Defaults locked**: scenario=`normalized` (col F per contract §8); Bad Debt to N62 only (hard-coded `_SPECIAL_SKIP_KEYS` skips `opex_bad_debt_expense` at N106 to avoid double-count); monthly grid annual-only (col N, B-M left blank); hardcoded monthly headers not overwritten; default skip set covers gap_*/header_only/derived/manual/*_pending statuses; include set defaults to {mapped, proposed}. `PopulateReport` dataclass surfaces per-concept outcomes (written / skipped / no_source / no_target / error) + aggregate summary + warnings. CLI: `python uw_template_writer.py <analyzer> <template> <output> [--scenario] [--template-version] [--registry]`. **`tests/test_uw_template_writer.py`** ships with two tests: empty-Analyzer smoke (bundled v0.2.14, verifies no crash, report shape, skip rules, special-key skip, 17-sheet round-trip — runs to completion; 2 cells written / 89 no_source / 20 skipped) and populated-Analyzer e2e (Homestead 176-bed fixture, gitignored — verifies EGI $7,001,957, EBITDARM $1,767,483, GPR $9,524,893 land at `T-12 Analysis!N{69,115,58}`; rent_roll path writes 176 rows at `Rent Roll Analysis!A211+` with first bed = "A1 / IL / Occupied / Janet (Francis) Pierson"; total 85 concepts written / 3627 cells populated). One warning on empty test: `Rent Roll Analysis!A210` is blank — repo's Sample Files template is a working copy predating the contract's row-210 header; canonical template in `Deals/Acquisition/_Template/` doesn't trigger this. Phase 2.5 (Streamlit UI integration into `app.py`) is the next deliverable. Earlier on 2026-05-25: Cross-track follow-up: **Substrate v0.2.14 + RR v1.18.1 + UWT v0.2.1** — closes the AI-column conflict surfaced by UWT v0.2.0 between the 2026-05-25 UW Template handoff contract (which reserves `Rent Roll Input!AI` for Deposit) and substrate v0.2.13 / RR v1.18.0 (which put Preleased Date at AI earlier the same day). Per user decision: Deposit gets AI; Preleased moves to AJ. **Substrate v0.2.14** (`tools/migration/migrate_to_v0214.py`, 4 surface patches, 8-check verify, idempotent): (a) `Rent Roll Input!AI4` header `"Preleased\nDate"` → `AJ4`, with style + column width copied; (b) per-cell relocation handler for any populated AI7:AI606 data → AJ7:AJ606 (bundled-file case is no-op); (c) new `Rent Roll Input!AI4` header = `"Deposit"`, styled to match X4 (Balance — $-typed input), col width 11.0; AI7:AI606 number_format `$#,##0.00` applied to 600 empty cells; (d) `RR_Input_Data` named range widened `'Rent Roll Input'!$A$7:$S$606` → `'Rent Roll Input'!$A$7:$AJ$606` — the legacy A:S scope predated v1.16.0+ ancillary cols. **Section N impact: zero** — Rent Roll Recon Section N (v0.2.13 BL-0025 exposure surface) matches on Status="Preleased" in col E, not on the Preleased Date column, so the AI → AJ relocation has no formula impact. **RR v1.18.1** (`analyzer_rr_writer.py`): `COL_AI_INDEX = 35` now documents the Deposit slot (clear-only — no parser support yet); new `COL_AJ_INDEX = 36` for Preleased routing; `SOURCE_COLUMNS_AI` → `SOURCE_COLUMNS_AJ`; Step-1 clear loop extended to defensively clear both AI (catches stale Preleased data on forward-rolled workbooks) and AJ. **app.py** `ANALYZER_SUBSTRATE_VERSION` constant bumped `"0.2.13"` → `"0.2.14"`. **Deposit parser support deferred** — `mappings.py` / `normalizer.py` / `CONDENSED_COLUMNS` unchanged; a source RR fixture with a Deposit column is needed before patterns can be designed. **UWT registry v0.2.1** (`tools/uw_template/registry.json`): `rr_preleased_date` source address AI → AJ; `rr_deposit` status `decided_pending_upstream` → new `substrate_ready_parser_pending`; new status added to legend + HTML pill class + filter; two `open_questions` closed (Preleased relocation, `RR_Input_Data` scope), one added (Preleased Date still has no UW Template v5 column even after relocation). Status counts unchanged (88 mapped / 10 gap_target / 5 gap_source / 4 proposed / 4 other). Earlier on 2026-05-25: Track 4 — UWT v0.2.0 Phase 1 ships: registry extended 72 → **111 concepts** modeling **three paste paths** per the 2026-05-25 handoff contract (`Deals/Acquisition/_Template/ALF Templates/Documentation & Maps/2026-05-25-UW-OUTPUT-HANDOFF-CONTRACT.md`). New paths: **rent_roll** (35 concepts — full crosswalk of `Rent Roll Input` cols A-AH → `Rent Roll Analysis` rows 211+, with position shifts and three template-side gaps: K Care Level tier label, S Period Date, AH Total Ancillary) and **ar** (4 aging concepts at UW cols N-Q, all gap_source pending upstream resident-key join). Substrate mapped-against bumped v0.2.9 → v0.2.11. Status rollup: 88 mapped / 10 gap_target / 5 gap_source / 4 proposed / 4 other (decided_pending_upstream, derived, header_only). **AI-column conflict logged**: 2026-05-25 contract assigns `Rent Roll Input!AI` to Deposit; v0.2.13 (also 2026-05-25) just put Preleased Date at AI. Per user decision, Deposit gets AI → Preleased Date relocates (likely AJ) — Track 1 + Track 3 cross-cutting follow-up filed in `registry.json` `open_questions`. Other open questions: `RR_Input_Data` named range scope (`A7:S606` too narrow for the cols V-AI paste), Preleased target in template v5, AR row-level routing prerequisite. Generator extended with Path filter (T-12 / Rent Roll / AR) and per-path coloured sections in mind-map HTML; markdown / CSV both group by path × category. Still no writer module — Phase 2. SPEC-UWT bumped to v0.2.0 with restructured three-path framing; CHANGELOG-UWT v0.2.0 entry seeded. Earlier on 2026-05-25: RR v1.18.0 + Substrate v0.2.13 — closes UW-BACKLOG **BL-0025**: move-out & preleased exposure surface. User reported on the Homestead v2 RR that "there are move outs, this isn't captured in the normalizer and not reflected in the analyzer. I need this captured for exposure." Cross-track session (Track 1 + Track 3, user-authorized at chat start) closes two related gaps: (1) Homestead's 3 "Vacant w/ Prelease" units (A4 / F1 / F2) were silently collapsing to plain `Vacant` because `\bvacant\b` matched first; (2) `Move-out Date` was captured into `Rent Roll Input!W` since v0.1.10 / RR v1.16.0 but no Analyzer formula read it, so no UW exposure view existed. **Parser side (RR v1.18.0):** `mappings.py` `DEFAULT_BED_STATUS` gets new rule `(r"\bprelease", "Preleased")` ordered immediately before `\bvacant\b`. `normalizer.py` `FIELD_PATTERNS` gets `prelease_date` matching bare `^preleased$` (Homestead's `Preleased` column header is the date the prelease was signed). `CONDENSED_COLUMNS` extended to 31 cols (col 31 = `Preleased Date`, AE on Condensed_RR). `analyzer_rr_writer.py` routes Preleased Date to `Rent Roll Input!AI` (AH=34 reserved for v0.1.13 Total Ancillary $ formula). `analyzer_rr_translator.py` docstring update only — `Preleased` passes through unchanged. **Substrate side (v0.2.13, via `migrate_to_v0213.py`):** three surface changes — (a) `Rent Roll Input!E7:E606` DV list `"Occupied,Vacant,Notice,Eviction"` → `"Occupied,Vacant,Notice,Eviction,Preleased"`, (b) `Rent Roll Input!AI4` = `"Preleased\nDate"` header (Q4-styled), (c) new Section N appended to `Rent Roll Recon` at rows 178-198 (pure append — no `insert_rows`, avoids BL-0001 qualified-range-endpoint trap). **N1 point-in-time exposure** (rows 180-189): Occupied / On notice / Vacant / Preleased / Total beds / Gross exposure (Notice + Vacant) / **Net exposure (Notice + Vacant − Preleased)** / Net exposure %. **N2 forward NTV departures** (rows 191-198): ≤30d / 31-60d / 61-90d / 91+d / No date or past / Total Notice sanity (= N1 row 183). Time windows computed against `Rent Roll Recon!B2` (period dropdown). 8-check migration verify, idempotent (gate: `Cover!B8 == "v0.2.13"` AND `Rent Roll Recon!A178` starts with "N"). **End-to-end on Homestead fixture** (period 2026-04-24, 176 beds): Status counts 128/40/5/3 (was 128/43/5/0 — the 3 preleased units split from Vacant). N1 Net exposure: 7 IL / 18 AL / 17 MC = 42 total (**23.9%**); gross 8/20/17 = 45. N2 ≤30d: 3 NTVs (Julius Mims 4/30, Peggy Salger 4/30, Thomas Winterbury 5/13 — all AL); No date / past: 2 NTVs (Hedenburg, Stowe). Sanity holds: N2 Total Notice = N1 On notice = 5. **Why net = Notice + Vacant − Preleased:** conventional ALF UW exposure framing — Preleased units offset gross because the lease is signed and the unit is on the way to being filled, not a risk-bearing vacancy. Both surfaced side-by-side. Sheet count unchanged at 16; all 16 AZ4 anchors stamped v0.2.13. Bundled `ALF_Financial_Analyzer_Only.xlsx` updated in place v0.2.12 → v0.2.13. Also: `ANALYZER_SUBSTRATE_VERSION` constant in `app.py` was lagging at `"0.2.11"` (v0.2.12 had shipped without updating it); v0.2.13 corrects to `"0.2.13"`. Earlier on 2026-05-25: Substrate v0.2.12 — closes UW-BACKLOG **BL-0024**: Dashboard blended-vs-segment formula mis-references. Track 5 (webapp Dashboard) build on 2026-05-24 discovered that the xlsx `Dashboard` sheet's three headline tiles — B6 OCCUPANCY, F20 ADR, K6 REVPOR — plus 9 derivative cells (B8 status text, C21 row, D35 card, E55 gap, G55 emoji, H55 risk text, P5 anchor, K8 status text, F21 row) were pulling from `'T12 Analytics'!F134` (AL-only occupancy `=C11/C6`), `F140` (MC-only ADR `=D20/(D11*12)`), or `F143` (MC-only RevPOR `=(D20+D27)/(D11*12)`) while their Dashboard labels said "Normalized community occupancy" / "Blended ADR" / "Normalized RevPOR" / "Community occupancy %". T12 Analytics col A even labels those F-cells "— AL" / "— MC" — the bug was on the Dashboard pulls, not on the T12 Analytics source. On the Homestead populated fixture: OCCUPANCY 64.5% (AL-only) vs blended 72.7% (128/176); ADR & REVPOR both $6,802 (MC-only) vs blended $4,546 / $4,562. v0.2.12 rewrites all 12 cells to compute blended directly from T12 Analytics column-E primitives (E6 total beds / E11 total occupied / E20 blended annual base rent / E27 blended LOC revenue): occupancy cells → `E11/E6`, F20 → `E20/(E11*12)`, REVPOR cells → `(E20+E27)/(E11*12)`. All wrapped in `IFERROR(...,"—")`; threshold-comparison shapes (B8/G55/H55) preserve existing ✓/⚠/✗ branches + "— Source not populated" fallback exactly. **Inline approach** (12 cells × E-primitive math) chosen over **add-rows-to-T12-Analytics** — inline keeps blast radius to Dashboard only, zero T12 Analytics surface change. Migration via `tools/migration/migrate_to_v0212.py` (12 cell rewrites + 17 version stamps, 5-check + 12-per-cell verify, idempotent — gate checks `Cover!B8 == "v0.2.12"` AND `Dashboard!B6` does NOT contain substring `"F134"`; each per-cell patch is also self-idempotent via a "current formula contains buggy F134/F140/F143 substring" check). 6 native Excel charts preserved (pre=6, post=6), 75 merged ranges preserved, hidden sheet states preserved (AR & Collections, Workbook Health), AR variance tile at K10:L13 unaffected (K11 formula + K13 footnote both intact from the v0.2.10/v0.2.11 hotfix). Bundled `ALF_Financial_Analyzer_Only.xlsx` updated in place v0.2.11 → v0.2.12. **Cross-pipeline impact:** the xlsx Dashboard tile numbers now match what `dashboard_model.py` (Track 5) computes in the Streamlit webapp Dashboard tab — resolving the discrepancy that surfaced the bug. **Only Pending backlog item: BL-0019** (persistent audit log, Track 1). Earlier on 2026-05-24: Track 5 — webapp Dashboard surface ships at v0.1.0 (followed by UI-polish bumps v0.1.1–v0.1.10 the same day). New `dashboard_model.py` + `dashboard_ui.py` modules surface the same data as the bundled Analyzer's `Dashboard` sheet inside the Streamlit webapp as a mobile-friendly tab via `st.tabs(["📊 Dashboard", "⬇️ Download"])`. Pure-Python compute mirrors T12 Analytics col-E aggregation (Description_Map label → totals over GLRows) since openpyxl can't evaluate formulas; zero new dependencies; 44-field `DashboardModel` dataclass; `st.metric` + `st.dataframe` + Altair donut/bar charts. **27-case regression test** at `tests/test_dashboard_model.py` reconstructs RR + T12 inputs from a populated Analyzer fixture (gitignored at `Sample Files/dashboard/regression_v0211.xlsx`) and asserts each metric matches the xlsx's `data_only=True` cached values — all 27 pass on Homestead Village. The three xlsx-Dashboard cross-reference bugs Track 5 surfaced (B6/F20/K6) — plus 9 derivative cells with the same pattern — are now closed by v0.2.12 above; regression test's `test_known_divergence_*` whitelists are ready to flip to equality once the fixture is rebuilt against v0.2.12. Track 5 docs: `SPEC-T5.md` + `CHANGELOG-T5.md` + `tests/fixtures/dashboard/README.md`. Also on 2026-05-24: hotfix on v0.2.10/v0.2.11 bundled Analyzer — Excel was throwing "Repair Result … Removed Records: Formula from /xl/worksheets/sheet2.xml part" on open. Root cause: two label strings in the AR-module migrations started with `=` (`Dashboard!K13` v0.2.11 footnote `"= T12 bad debt − annualized AR write-offs"` and `AR & Collections!B47` v0.2.10 label `"= Implied closing AR"`). openpyxl's value setter classifies any leading-`=` string as a formula and writes it into `<f>`; Excel then strips the K13 one on open (couldn't parse it) and renders B47 as `#NAME?`. Both label strings rewritten with the leading `"= "` dropped — the "this row equals the formula next to it" relationship is structurally obvious from layout. Migration scripts (`migrate_to_v0210.py`, `migrate_to_v0211.py`) patched with inline comments + docstring notes so future label adds avoid the same trap; bundled `ALF_Financial_Analyzer_Only.xlsx` re-written in place (styling preserved). New 5th openpyxl quirk added to the "openpyxl quirks that bite migrations" section below. Whole-workbook re-scan shows zero remaining text-shaped formula cells. Dashboard!K11 AR variance formula was always valid and survived Excel's repair pass intact. Substrate stamp unchanged at v0.2.11. Earlier on 2026-05-23: Track 4 — UW Template integration Phase 0 ships: 72-concept modular mapping registry at `tools/uw_template/registry.json` against `ALF_UW_Template_v4.xlsx`, generator script + interactive HTML mind-map + MD/CSV trackers, `SPEC-UWT.md` + `CHANGELOG-UWT.md` seeded. UWT v0.1.0 — registry + docs only, no writer yet. 57/72 concepts cleanly mapped; 15 concepts flagged for Phase 1 disposition (Bad Debt placement, 2nd Person Revenue, monthly grid widening, EBITDA row, occupied beds, RR Analysis date format, monthly header overwrite). Earlier on 2026-05-23: substrate v0.2.10 + v0.2.11 — opens AND closes UW-BACKLOG **BL-0023** (AR & Collections module). New `AR & Collections` analytical sheet at index 8 (hidden by default; revealed when an AR aging file is uploaded). New `ar_normalizer.py` + `ar_writer.py` modules give the Streamlit app a third operator input (AR aging .xlsx/.csv) alongside RR and T12. Workbook Health B43 wrapped in AR-presence IF guard — `=IF('AR & Collections'!Z1=1, AR.C15, SUM('Rent Roll Input'!$X))` — so the RR-derived fallback is preserved bit-for-bit when no AR uploaded. P5 pre-export gate added at WH row 52 (inert "✓" when Z1=0, compares AR as-of to `RR_Period_Date` when Z1=1); READY-FOR-EXPORT summary shifted from row 52 → row 53 with B52 ANDed in. v0.2.11 adds Dashboard variance tile at K10:L13 + Cover AR module version line at A11/B11. `mappings.py` `DEFAULT_PAYER` extended with Managed Care + Medicare-Advantage / MCO rules (MA rules ordered BEFORE bare `\bmedicare\b`); `PAYER_FALLBACK` constant unchanged — AR ingest constructs `MappingSet(payer_fallback="Self-Pay + Other")` per-instance, RR behavior preserved. Bundled `ALF_Financial_Analyzer_Only.xlsx` forward-applied v0.2.4 → v0.2.10 → v0.2.11 directly per BL-0021 carry-forward (still skips intermediate v0.2.5-v0.2.9 substrate features). Live operator AR sample **PENDING** — built against synthetic at `tests/fixtures/ar/ar_synthetic_v01.xlsx` (12 residents × 14 cols, exercises all 7 payer buckets); fuzzy header rules will need expansion when real samples arrive. Cowork-authored design handoff (`2026-05-23-AR-Collections-Claude-Code-Handoff.md`) reviewed against codebase, 12 spec issues raised and decided here; handoff-back-to-Cowork block produced for spec Rev 2. **Only Pending backlog item: BL-0019** (persistent audit log, Track 1).)

---

## What this repo is

Two parallel pipelines feeding one underwriting workbook:

1. **Rent Roll Normalizer (RR)** — Streamlit app that turns any operator's rent roll into a normalized bed-level Excel output.
2. **T12 Normalizer (T12)** — parser + writer that turns various T12 financial-statement formats into a normalized monthly trending Excel.

Both pipelines populate a shared destination workbook: `ALF_Financial_Analyzer_Only.xlsx` (the "Analyzer"). The Analyzer is the underwriting substrate — RR drives Rent Roll Input, T12 drives T12 Input, and the analytical sheets reconcile + roll up to UW Output.

A separate downstream full-underwriting sheet (not in this repo) consumes UW Output. Optimizing the Analyzer to be a clean handoff to that sheet is an active workstream.

**Two product lines (added 2026-05-20).** Everything above is the **ALF** (senior-housing) product. A second **MF** (multifamily / conventional apartments) product line is being built in phases. The app gates on an **ALF/MF mode selector** shown right after login (`app.py`, just after `require_login()`), driven by `auth.allowed_modes(username)`. **Phase 0 (shipped):** the selector + access seam — every logged-in user currently sees both modes; MF renders a "coming soon" placeholder (`_render_mf_placeholder()`) and `st.stop()`s before the ALF pipeline, which runs **unchanged** in ALF mode. Per-user access-type gating (limiting a user to ALF-only or MF-only via a future `[auth.access]` secrets table) is deferred — `allowed_modes()` is the single seam where it lands. **Future MF phases (not built):** P1 MF RR normalizer → standalone MF workbook; P2 MF T12 + AR intake; P3 OM/comps extraction; P4 MF Analyzer + UW Template. MF has **no Analyzer** and shares none of the ALF care/payer/acuity data model — it's units/floorplans/leases. Sample data: `MF Docs/` (property *Hidden Lakes*: RR / T12 / AR / Sortable-RR). MF docs/specs will be added as those phases land; do not assume ALF conventions apply to MF.

**MF naming convention (locked 2026-05-20 — Option C, asymmetric prefix).** ALF code stays unprefixed (ALF = default, by virtue of being the original). **New MF code lives at the repo root with the `mf_` prefix**, mirroring the ALF module shape one-for-one. Shared utilities stay unprefixed. The MF naming convention:

| Concern | ALF (existing) | MF (planned) |
| --- | --- | --- |
| RR parser | `normalizer.py` | `mf_normalizer.py` |
| RR mappings (closed vocabularies) | `mappings.py` | `mf_mappings.py` |
| RR pre-cleaner | `pre_cleaner.py` | `mf_pre_cleaner.py` (if MF RR needs banner stripping) |
| RR reports | `reports.py` | `mf_reports.py` |
| Standalone RR workbook writer | `writer.py` | `mf_writer.py` |
| Analyzer RR translator | `analyzer_rr_translator.py` | `mf_analyzer_rr_translator.py` (Phase 4) |
| Analyzer RR writer | `analyzer_rr_writer.py` | `mf_analyzer_rr_writer.py` (Phase 4) |
| T12 parser | `t12_normalizer.py` | `mf_t12_normalizer.py` (parallel — MF chart of accounts differs even if file shape is similar) |
| T12 writer | `t12_normalizer_writer.py` | `mf_t12_normalizer_writer.py` |
| AR parser | *(none)* | `mf_ar_parser.py` |
| OM extractor | *(none)* | `mf_om_extractor.py` |
| Analyzer substrate (workbook) | `ALF_Financial_Analyzer_Only.xlsx` | `MF_Financial_Analyzer_Only.xlsx` (Phase 4) |
| Substrate migrations | `tools/migration/migrate_to_v0NN.py` | `tools/migration/mf_v0NN.py` (independent version stream) |
| Spec / changelog | `SPEC-RR.md` / `SPEC-T12.md` / `CHANGELOG-RR.md` / `CHANGELOG-T12.md` | `SPEC-MF.md` / `CHANGELOG-MF.md` |
| Shared (no prefix) | `auth.py` / `branding.py` / `property_name.py` / `period_date.py` / `app.py` | same — shared utilities stay unprefixed |
| Sample data | `Sample Files/` (gitignored) | `MF Docs/` (gitignored) |
| UW-BACKLOG | `UW-BACKLOG.md` | same file; BL-NNNN numbering is continuous across product lines (no separate MF numbering) |

**When the root gets crowded** (probably Phase 3-4 once 6+ `mf_*.py` files exist) — refactor into Option A subdirectory packages (`alf/` + `mf/`) then. The asymmetric prefix is explicitly a *near-term* choice that defers the subdirectory move until MF has enough code to justify it.

## Local clone path

`C:\One Drive Business\OneDrive - (na)\office\rent_roll_app` — Windows machine, PowerShell. **The repo lives inside OneDrive Business** (relocated 2026-05-21 from a former local-only location, which is dead — do not work from any old local copy outside OneDrive). Because git runs inside an actively-syncing OneDrive folder, follow the safety guidance in [`ONEDRIVE-WORKFLOW.md`](ONEDRIVE-WORKFLOW.md): run `tools\check_onedrive_sync.ps1` before git operations, push promptly after each commit, and never run destructive git ops mid-sync.

## Live deploy

https://rrnormalizer.streamlit.app/ — Streamlit Community Cloud, auto-deploys from `origin/main` on push (~30-60 sec lag). **Reboot-first rule:** if live behavior diverges from a verified local run on the same file, assume stale module cache and reboot from share.streamlit.io before debugging.

---

## Workstream tracks

The repo runs four parallel tracks. They share an Analyzer / UW pipeline but are otherwise independent. **Track N chats do not edit Track M files** without explicit cross-track authorization — see "Scope discipline" below.

### Track 1 — RR Normalizer (RR-side code)

| What | Where |
| --- | --- |
| Code | `app.py`, `normalizer.py`, `mappings.py`, `pre_cleaner.py`, `period_date.py`, `reports.py`, `writer.py` |
| Spec | `SPEC-RR.md` |
| Changelog | `CHANGELOG-RR.md` |
| Current version | RR v1.19.0 |

### Track 2 — T12 Normalizer (T12-side code + Analyzer substrate)

| What | Where |
| --- | --- |
| Code | `t12_normalizer.py`, `t12_normalizer_writer.py`, plus T12 sections of `app.py` |
| Spec | `SPEC-T12.md` |
| Changelog | `CHANGELOG-T12.md` |
| Bundled workbook | `ALF_Financial_Analyzer_Only.xlsx` |
| Migration scripts | `tools/migration/migrate_to_v01N.py` (one per substrate version) |
| Verification harness | `tools/verify_t12_v020.py` (parser-side; runs all four reference fixtures) |
| Current code version | T12 v0.2.1 |
| Current substrate version | v0.2.14 bundled (chain through v0.2.14; bundled forward-applied v0.2.4 → v0.2.10 → v0.2.11 → v0.2.12 → v0.2.13 → v0.2.14 directly per BL-0021 — skips intermediate v0.2.5-v0.2.9 substrate features) |
| AR module code version | AR v0.1.0 (`ar_normalizer.py` + `ar_writer.py` — built against synthetic fixture, live operator sample pending) |

**Module naming gotcha (updated 2026-05-15 after BL-0011 — Track 1 disambiguation now fully complete at file + function + class level).** Four modules historically shared a `t12_` prefix because the destination workbook was originally a standalone T12 intake template — the prefix meant "operates on the T12-shaped destination workbook," not "operates on T12 data." Once the bundled Analyzer flow shipped (RR v1.12.0) the prefix became misleading. The two Track 1 modules have now been renamed; the remaining `t12_*` files are the legitimate T12-data modules. All four are imported by `app.py` and serve distinct roles:

| File | Function | Role |
| --- | --- | --- |
| `analyzer_rr_translator.py` | `translate_for_t12()` | Translates Condensed_RR vocabulary → Analyzer data-validation vocabulary (e.g. `1BR` → `1 Bedroom`). RR-side (Track 1). Was named `t12_translator.py` until 2026-05-14 (BL-0010) — see CHANGELOG-RR.md for the rename. The function name `translate_for_t12()` is the last `t12_` symbol on the Track 1 side; left alone for now since `for_t12` reads as "for the destination" rather than "for T12 data" — rename only if it becomes a confusion source. |
| `analyzer_rr_writer.py` | `populate_rr_input()` | Writes the translated RR into the Analyzer's `Rent Roll Input` sheet. RR-side (Track 1). Was named `t12_writer.py` until 2026-05-10. The function `populate_t12()` was renamed to `populate_rr_input()` and the exception class `T12CapacityError` was renamed to `AnalyzerRRCapacityError` on 2026-05-15 (BL-0011). |
| `t12_normalizer.py` | `parse_t12()` | Parses raw T12 financial statements (Yardi / MRI / BrokerFinancialSummary format registry). T12-side (Track 2). |
| `t12_normalizer_writer.py` | `populate_t12_input()` | Writes parsed T12 GL detail into the Analyzer's `T12 Input` sheet. T12-side (Track 2). |

If a future chat is tempted to delete one as "duplicate," check `app.py` lines 46-51 (imports) and the orchestration block around 880-940 — all four are wired in. The two surviving `t12_*` files now legitimately operate on T12 data; the prior cross-track artifacts are all cleaned up.

### Track 3 — Analyzer optimization (workbook-only, no code)

Optimizing the Analyzer's structure for the downstream UW handoff. Workbook edits, new sheets, named ranges, cell comments. Does not touch RR or T12 code. Workstream began 2026-05-07.

| What | Where |
| --- | --- |
| Decisions log | `OPTIMIZATION-DECISIONS.md` |
| Roadmap | 4 branches: 1 Correctness, 2 Handoff, 3 Analytical coverage, 4 Substrate. Branches 1+4 closed in v0.1.6. Branch 3 next. Branch 2 last. |

### Track 4 — ALF UW Template integration (downstream consumer)

Wiring the Analyzer's `UW Output` / `UW Export` surface into the downstream **ALF UW Template** workbook (per-deal populated copy). Workstream began 2026-05-23. Phase 0 (this initial release) is inspection + mapping only — no writer yet.

| What | Where |
| --- | --- |
| Spec | `SPEC-UWT.md` |
| Changelog | `CHANGELOG-UWT.md` |
| Handoff contract (Analyzer side) | `UW-OUTPUT-HANDOFF-CONTRACT.md` |
| Mapping registry (data) | `tools/uw_template/registry.json` |
| Artifact generator | `tools/uw_template/build_mapping_artifacts.py` |
| Mind-map visualizer | `tools/uw_template/mapping_mindmap.html` |
| Tracker (human-readable) | `tools/uw_template/MAPPING_TRACKER.md` |
| Tracker (diffable CSV) | `tools/uw_template/mapping_tracker.csv` |
| Handoff tracker (index) | `tools/uw_template/HANDOFF_TRACKER.md` |
| Handoff brief template | `tools/uw_template/HANDOFF_TEMPLATE.md` |
| Per-handoff briefs | `tools/uw_template/handoffs/YYYY-MM-DD-<slug>.md` |
| Template source file | `assets/ALF_UW_Template_v5.xlsx` (committed blank reference, binding from 2026-05-26). v4 retained for backward compat at `Sample Files/ALF_UW_Template_v4.xlsx` (gitignored — working-copy reference only). |
| Current code version | UWT v0.8.0 (default template flipped to v6 — T-12 income restructure; `_finalize_t12_layer3` + `_write_section_i_raw` version-dispatched via `_T12_LAYOUT`) |
| Evaluator module | `uw_output_model.py` — `compute_uw_output_values()` → annual `{concept_key: value}` + `compute_uw_output_monthly()` → `{concept_key: [12 floats]}`; reuses `dashboard_model` aggregation; passed to writer as `computed_values=` / `computed_monthly=` |
| Dynamic-array repair | `uw_template_writer._restore_dynamic_arrays(output_bytes, template_bytes)` — pure zipfile+re; restores metadata.xml + per-cell `cm` markers post-save (openpyxl quirk #6) |
| Mapped against template | `v6` default (v5/v4 still supported via `template_version='v5'`/`'v4'`) |
| Mapped against substrate | v0.2.14 — unchanged; v5 didn't move the substrate, only template-side rows/cols. Deposit slot reserved at `Rent Roll Input!AI` (clear-only, no parser support yet); Preleased Date at AJ; `RR_Input_Data` widened to `$A$7:$AJ$606`. |
| Paste paths | (1) UW Output → T-12 Analysis · (2) Rent Roll Input rows 7+ → Rent Roll Analysis rows 211+ · (3) AR & Collections → Rent Roll Analysis cols N–Q (gap_source, gated on resident-key join) |
| Authoritative handoff doc | `Deals/Acquisition/_Template/ALF Templates/Documentation & Maps/2026-05-25-UW-OUTPUT-HANDOFF-CONTRACT.md` (external — maintained outside repo) |

**Modular registry pattern.** `registry.json` is keyed on semantic concepts (`egi`, `labor_care_staff`, `licensed_beds_il`, ...) with version-keyed targets (`targets.v4 = {...}`). Adding template `v5` later means extending the registry — no code change to the generator or future writer. Re-run `python tools/uw_template/build_mapping_artifacts.py` after any registry edit to regenerate the HTML / MD / CSV artifacts.

**Handoff protocol (template author lives outside this repo).** The ALF UW Template is authored externally in Excel (operator → Cowork → Excel); Claude Code owns the **registry** and the **writer**, not the template file. When Track 4 work surfaces a template-side change (a new `gap_target` concept, a needed column/row, a styling decision, a structural addition), the chat that surfaces it produces a **handoff brief** under `tools/uw_template/handoffs/YYYY-MM-DD-<slug>.md` (copy from `HANDOFF_TEMPLATE.md`) and adds a row to the top of the index table in `HANDOFF_TRACKER.md`. The operator picks up the brief, authors the change in Excel (optionally via Cowork), and drops the new file back at `Sample Files/ALF_UW_Template_v5.xlsx`. The next Claude Code chat extends the registry's `templates.v5 = {...}` block, adds `targets.v5` keys per affected concept, re-runs `build_mapping_artifacts.py`, smoke-tests the writer against the new version, and marks the handoff **Verified**. Mapping updates ride **inline** in each handoff (always coupled to a template change); the existing `MAPPING_TRACKER.md` / `mapping_tracker.csv` / `mapping_mindmap.html` remain the source of truth for the full mapping state. Trigger criteria for a new handoff, status legend, and the full protocol live in `HANDOFF_TRACKER.md` itself.

### Track 5 — Webapp Dashboard surface (`dashboard_model.py` + `dashboard_ui.py`)

Surfacing the Analyzer's `Dashboard` data inside the Streamlit webapp as a mobile-friendly tab — same metrics the downloaded xlsx's `Dashboard` sheet shows, rendered in-browser so the user doesn't have to open Excel. Workstream began 2026-05-24.

| What | Where |
| --- | --- |
| Spec | `SPEC-T5.md` |
| Changelog | `CHANGELOG-T5.md` |
| Compute layer (pure Python) | `dashboard_model.py` — `compute_dashboard()` + `DashboardModel` |
| Renderer (Streamlit) | `dashboard_ui.py` — `render_dashboard()` |
| App seam | `app.py` — `st.tabs(["📊 Dashboard", "⬇️ Download"])` after the analyst review tabs |
| Regression test | `tests/test_dashboard_model.py` (27 cases) |
| Test fixture (gitignored) | `Sample Files/dashboard/regression_v0211.xlsx` |
| Current code version | T5 v0.1.0 |
| Target substrate | v0.2.11 |

**Why pure Python.** openpyxl can't evaluate formulas — `data_only=True` reads return `None` for every formula cell on a Python-written workbook. The xlsx Dashboard is a formula-reference layer over `T12 Analytics` (which aggregates `T12 Raw Data` per Description_Map label + `Rent Roll Recon` for bed counts). Re-implementing the ~60 metrics in Python is bounded (simple arithmetic over already-parsed `NormalizeResult` + `T12ParseResult` + `AROutput`), zero new dependencies, and the regression test against the xlsx fixture is the drift guard.

**Known divergences from xlsx (Python correct).** Three Dashboard cells reference single-care-type cells while being labeled as blended on the Dashboard — `B6` (occupancy, pulls AL-only `F134`), `F20` (ADR, pulls MC-only `F140`), `K6` (RevPOR, pulls MC-only `F143`). Python computes structurally-correct blended values. A Track 3 substrate fix is queued (separate spawned task) to align the xlsx.

---

## Cross-cutting docs

| File | What it is |
| --- | --- |
| `journal.md` | Per-chat session log, newest at top. **Read the top entry before starting a new chat** — it usually has carry-forwards and known follow-ups. |
| `README.md` | Public-facing. Stale: still RR-only framing, doesn't mention T12 or the bundled-Analyzer flow. Update is a known follow-up but not a priority. |
| `ARCHITECTURE.md` | Full workflow + data-flow map: module inventory, public contracts (CONDENSED_COLUMNS, GLRow/T12ParseResult, substrate write-targets), Mermaid diagrams of both pipelines + the substrate + migration chain, and the modularity/extension points. Read when you need the system shape rather than the history. |
| `COSMETIC-CHANGES.md` | Tracker for visual/branding-only changes to the Streamlit app (color scheme, logo, fonts). Does not move the RR/T12/substrate version counters. Newest at top. |
| `CLAUDE.md` | This file. |

---

## Scope discipline (matters)

Per the journal 2026-05-06 retrospective, the project enforces "one track at a time" per chat. **A T12 chat does not edit RR files. An RR chat does not edit T12 files. A Track 3 chat does not touch any code.**

When a chat starts on track A and the user pivots toward track B, the assistant should stop and confirm: "We're now in [track B] territory. Fresh chat, or proceed knowing we're cross-cutting?" — rather than silently crossing.

Conversational examples should label placeholder text as `<REPLACE THIS>` so the user doesn't paste literal placeholders into commit messages (see `be3b134` for the cautionary tale).

---

## Open carry-forwards (refreshed 2026-05-21, post-substrate v0.2.9 chart-link fixes)

**The authoritative forward-looking list lives in [`UW-BACKLOG.md`](UW-BACKLOG.md).** Read that file for what's pending. The historical "closed" notes below stay here purely for traceability of how prior chats deferred work; the "Medium / Low priority" sub-sections that used to live here have been removed because they had drifted (e.g. "Branch 2 — Handoff readiness" was open for weeks here while it had already shipped as BL-0009 / substrate v0.2.0). When you want to know what's open, check UW-BACKLOG.md, not this section.

### Closed 2026-05-25 (RR v1.18.0 + Substrate v0.2.13 — Move-out & preleased exposure surface · BL-0025)

- ✓ **Three coordinated pieces close BL-0025.** User-reported gap: Homestead's 3 "Vacant w/ Prelease" units (A4 / F1 / F2) were silently collapsing to plain `Vacant` (the `\bvacant\b` rule fired before any prelease rule), and `Move-out Date` was captured into `Rent Roll Input!W` since v0.1.10 / RR v1.16.0 but no Analyzer formula read it — no UW "exposure" view existed. Cross-track work (Track 1 + Track 3) user-authorized at chat start.
- ✓ **Parser side (RR v1.18.0).** `mappings.py` `DEFAULT_BED_STATUS` gets new rule `(r"\bprelease", "Preleased")` ordered immediately before `\bvacant\b` so "Vacant w/ Prelease" hits Preleased first. `normalizer.py` `FIELD_PATTERNS` gets `prelease_date` matching bare `^preleased$` (Homestead's `Preleased` column header is the date the prelease was signed). `CONDENSED_COLUMNS` extended to 31 cols (col 31 = `Preleased Date`, AE on Condensed_RR); condensed-DataFrame construction at the line ~1206 dict also extended. `analyzer_rr_writer.py` routes Preleased Date to `Rent Roll Input!AI` via `COL_AI_INDEX = 35` + `SOURCE_COLUMNS_AI = ["Preleased Date"]`, idempotent clear of AI7:AI606 (AH=34 reserved for v0.1.13 Total Ancillary $ formula — DO NOT clear). `analyzer_rr_translator.py` docstring update only — `Preleased` passes through unchanged (no STATUS_MAP entry needed).
- ✓ **Substrate side (v0.2.13, via `migrate_to_v0213.py`).** Three surface changes: (a) `Rent Roll Input!E7:E606` DV list `"Occupied,Vacant,Notice,Eviction"` → `"Occupied,Vacant,Notice,Eviction,Preleased"`; (b) `Rent Roll Input!AI4` = `"Preleased\nDate"` header (Q4-styled, column width copied from Q); (c) new Section N appended to `Rent Roll Recon` at rows 178-198 (pure append — no `insert_rows`, avoids BL-0001 qualified-range-endpoint trap on the existing cross-sheet formulas). **N1 point-in-time exposure** (rows 180-189): Occupied / On notice / Vacant / Preleased / Total beds / Gross exposure (Notice + Vacant) / **Net exposure (Notice + Vacant − Preleased)** / Net exposure %. **N2 forward NTV departures** (rows 191-198): ≤30d / 31-60d / 61-90d / 91+d / No date or past (residual = N1 Notice − sum of dated buckets) / Total Notice sanity (= N1 row 183). All N2 windows compute against `Rent Roll Recon!B2` (period dropdown). 8-check verify, idempotent (gate: `Cover!B8 == "v0.2.13"` AND `Rent Roll Recon!A178` starts with "N"). Section N rows mirror Section A's COUNTIFS rather than `=B7` cross-row pulls — stays self-contained against a future Section A refactor.
- ✓ **End-to-end on Homestead fixture** (period 2026-04-24, 176 beds). Status counts: 128 Occupied / 40 Vacant / 5 Notice / 3 Preleased (was 128/43/5/0 — 3 preleased units split from Vacant). **N1 Net exposure:** 7 IL / 18 AL / 17 MC = 42 total (**23.9%**); gross 8/20/17 = 45; net % by care 11.3% / 29.0% / 32.7%. **N2 ≤30d:** 3 NTVs (Julius Mims 4/30, Peggy Salger 4/30, Thomas Winterbury 5/13 — all AL); **No date / past:** 2 NTVs (Hedenburg, Stowe — neither has a scheduled date). Sanity holds: N2 Total Notice = N1 On notice = 5.
- ✓ **Net = Notice + Vacant − Preleased.** Conventional ALF UW exposure framing. Preleased units offset gross because the lease is signed and the unit is on the way to being filled, not a risk-bearing vacancy. Gross + Net surfaced side-by-side (rows 187 + 188) so a reader sees the offset rather than just the net.
- ✓ **Substrate constant correction.** `ANALYZER_SUBSTRATE_VERSION` in `app.py` was lagging at `"0.2.11"` (v0.2.12 had shipped without updating it). v0.2.13 corrects to `"0.2.13"`.
- ✓ **Bundled file.** `ALF_Financial_Analyzer_Only.xlsx` updated in place v0.2.12 → v0.2.13. Sheet count unchanged at 16; all 16 AZ4 anchors stamped v0.2.13. Idempotency confirmed (re-run → "No-op").

### Closed 2026-05-25 (Substrate v0.2.12 — Dashboard blended-vs-segment formula fixes · BL-0024)

- ✓ **Twelve Dashboard cells rewritten to compute blended from T12 Analytics E-column primitives.** Three headline tiles (B6 OCCUPANCY, F20 ADR, K6 REVPOR) plus 9 derivative cells (B8 status text, C21 row, D35 card, E55 gap, G55 emoji, H55 risk text, P5 anchor, K8 status text, F21 row) were referencing segment-specific `'T12 Analytics'!F134` (AL-only), `F140` (MC-only), or `F143` (MC-only) while their Dashboard labels said "Normalized community occupancy" / "Blended ADR" / "Normalized RevPOR" / etc. New formulas: occupancy cells → `'T12 Analytics'!E11/'T12 Analytics'!E6`, F20 → `'T12 Analytics'!E20/('T12 Analytics'!E11*12)`, REVPOR cells → `('T12 Analytics'!E20+'T12 Analytics'!E27)/('T12 Analytics'!E11*12)`. All wrapped in `IFERROR(...,"—")`; threshold-comparison shapes (B8/G55/H55) preserve existing ✓/⚠/✗ branches + "— Source not populated" fallback exactly. Inline-in-Dashboard approach (chosen over adding new Blended ADR / Blended RevPOR rows to T12 Analytics Section 5) keeps blast radius to Dashboard only — zero T12 Analytics surface change. **Homestead populated fixture before/after:** OCCUPANCY 64.5% → 72.7%; ADR $6,802 → $4,546; REVPOR $6,802 → $4,562. Migration via `tools/migration/migrate_to_v0212.py` (12 cell rewrites + 17 version stamps, 5-check + 12-per-cell verify, idempotent). 6 native Excel charts preserved, 75 merged ranges preserved, hidden sheet states preserved, AR variance tile at K10:L13 unaffected. Bundled `ALF_Financial_Analyzer_Only.xlsx` updated in place v0.2.11 → v0.2.12. **Cross-pipeline impact:** Track 5 webapp Dashboard tab (which computes blended correctly via `dashboard_model.py`) now matches the downloaded xlsx Dashboard — the discrepancy that surfaced the bug is resolved.

### Closed 2026-05-23 (Substrate v0.2.10 + v0.2.11 — AR & Collections module · BL-0023)

- ✓ **AR & Collections module shipped end-to-end** — third operator input (AR aging .xlsx/.csv) alongside RR and T12, with full upload → parse → write → populated-Analyzer pipeline behind the Streamlit UI. Four files of new code (`ar_normalizer.py`, `ar_writer.py`, `tests/fixtures/ar/ar_synthetic_v01.xlsx`, `tests/fixtures/ar/README.md`); two new migration scripts (`tools/migration/migrate_to_v0210.py`, `tools/migration/migrate_to_v0211.py`); modifications to `mappings.py` (additive — Managed Care bucket + MA/MCO rules, RR `PAYER_FALLBACK` unchanged), `app.py` (imports, version constants, sidebar AR uploader + conditional as-of date, orchestration Step 3, filename builder, caption, error handler, version-footer bump, stale `ANALYZER_SUBSTRATE_VERSION` constant corrected "0.2.4" → "0.2.11"), and the bundled `ALF_Financial_Analyzer_Only.xlsx` (forward-applied v0.2.4 → v0.2.10 → v0.2.11 directly).

- ✓ **Substrate v0.2.10 (`migrate_to_v0210.py`)** — New `AR & Collections` sheet at index 8 (between Monthly Trending and UW Output), HIDDEN by default. 163 cells across 5 spec sections (Aging Summary / KPIs / By-Payer Mix / Roll-Forward & Bad-Debt / Flags). `Z1` = AR presence flag (0=no data, 1=populated). Workbook Health!B43 wrapped in IF guard reading `Z1`: when Z1=0 → original `SUM('Rent Roll Input'!$X)` (bit-for-bit preserved); when Z1=1 → reads AR sheet `C15` Total AR. P5 pre-export gate added at WH row 52 ("AR period matches RR period — inert if no AR"); READY-FOR-EXPORT summary shifted row 52 → row 53 with B52 ANDed in. Verified zero external refs to WH!B52 before the shift. Cross-sheet pins: Monthly Trending!N26 (annualized EGI), T12 Analytics!E7 (avg occupied beds), T12 Analytics!E98 (bad debt expense), RR_Period_Date (named range). 19-check verify, idempotent, regression-clean.

- ✓ **Substrate v0.2.11 (`migrate_to_v0211.py`)** — Dashboard variance tile at K10:L13 (previously empty): K10:L10 title "BAD DEBT VARIANCE" (REVPOR-style navy fill), K11:L12 merged value formula `=IF('AR & Collections'!Z1=0,"— upload AR to populate",'AR & Collections'!C56)` (dormant when no AR, live ⚪/✓/⚠ when uploaded), K13:L13 footnote. Cover row 11 AR Module version line at A11/B11 (sits in the existing blank row between T12 Normalizer at R10 and Links section at R12 — no row inserts). 11-check verify, idempotent, regression-clean.

- ✓ **`ar_normalizer.py` (AR module v0.1.0)** — Public API: `parse_ar_file(path_or_buffer) -> AROutput`. Fuzzy header matcher (13 regex rules, first-match-wins, ordered specific→generic so "Over 90" disambiguates from "Over 60"). Per-row sum-check with ±0.01 tolerance. `_coerce_number` handles `$`, `,`, and `(parens-as-negative)`. Accepts both .xlsx (path or BytesIO with `.name`) and .csv inputs — verified 4 source shapes (CSV/XLSX × path/BytesIO) parse identically. Constructs `MappingSet(payer_fallback="Self-Pay + Other")` per-instance unless caller passes own MappingSet. `AROutput` + `ARRow` dataclasses define the contract `ar_writer.py` consumes.

- ✓ **`ar_writer.py`** — `populate_ar_collections(analyzer_bytes, ar_output, ...) -> bytes` matching RR/T12 writer pipeline shape. Writes Z1=1, optional C3 as-of override, all 5 §1 aging totals, all 7 §3 payer rows, §4 roll-forward optionals (only if present), §5 flag cells (computes payer-concentration flag inline; RR-join flags C62/C63 stubbed to 0). Flips sheet_state hidden → visible. Raises `AROutputError` when AR & Collections sheet absent (substrate < v0.2.10).

- ✓ **`app.py` Streamlit wiring** — AR file_uploader (.xlsx/.csv) added to sidebar after T12 block, with conditional as-of date override defaulting to the RR period date. Orchestration Step 3 after RR+T12 writers; AR is optional throughout (no gating; non-AR downloads unchanged). Filename builder refactored to a `name_parts` list that appends "AR" tag when AR uploaded. Version footer now `RR vX · T12 vY · AR vZ`. Error handler routes `AROutputError` with a clear "substrate v0.2.10+ required" message for users of override Analyzers.

- ✓ **`mappings.py` extension** — Added Managed Care bucket + rules for Medicare Advantage, MA Plan, MCO. MA rules placed BEFORE bare `\bmedicare\b` (first-match-wins, so MA receivables don't get tagged as traditional Medicare). `PAYER_FALLBACK` constant unchanged — AR uses `MappingSet(payer_fallback="Self-Pay + Other")` per-instance, RR behavior preserved. All 14 `normalize_payer` test cases pass (existing + new + AR-instance fallback).

- ✓ **Cowork handoff review** — User-supplied `2026-05-23-AR-Collections-Claude-Code-Handoff.md` (Rev 1) reviewed against codebase. 12 spec issues raised: terminology (xlsx vs webapp), payer taxonomy mismatch (spec 6 buckets vs Dashboard 6 vs mappings.py 5+fallback), impossible sheet position ("after T12 Analytics, before Dashboard" since Dashboard is index 1), Workbook Health AR-replace claimed additive but actually disruptive, 3 Dashboard tiles requested but K10:L13 only fits one, missing cell pins, etc. All 12 decided here to fit current webapp flow; handoff-back-to-Cowork block produced for spec Rev 2. The webapp is the canonical implementation; spec catches up next.

- ✓ **Live operator AR sample PENDING** — built against synthetic `tests/fixtures/ar/ar_synthetic_v01.xlsx` (12 residents × 14 cols, exercises all 7 mappings.py payer buckets incl. new Managed Care via "Medicare Advantage" / "MCO" / "UHC MA Plan" rows). Fuzzy header rules will need expansion when real operator headers arrive. Sample-file convention: live operator samples go in `Sample Files/` (gitignored), synthetic stays in `tests/fixtures/ar/` (committed) as structural reference.

- ✓ **Deferred (intentionally out of scope for v0.1.0):** AR↔RR row-level join for §5 C62/C63 flags (resident-in-90+-with-concession, vacant-with-non-zero-AR) — needs ar_writer extension to read Rent Roll Input from the same workbook. Stubbed to 0. Standalone CHANGELOG-AR.md (consolidated into CHANGELOG-T12.md under v0.2.10/v0.2.11 for now).

### Closed 2026-05-21 (Substrate v0.2.9 — Dashboard chart-link fixes on the chain · BL-0020)

- ✓ **`migrate_to_v029.py` ports BL-0020's three chart-data-link fixes onto the migration chain.** Problem it solved: `migrate_to_v027.py` inserts the Dashboard from `v027_assets/dashboard_template.xlsx`, which has three buggy chart links — so any workbook forward-rolled through the chain got broken charts, even though the bundled file (user's hand-edited copy) had them fixed. v0.2.9 surgically corrects: Fix 1 — `Dashboard!C97:C108` EGI series `Monthly Trending` row 21 (Housekeeping) → row 26 (EGI); Fix 2 — `Dashboard!F90:F93` Payer Mix pie `Rent Roll Recon` col B (unit counts) → col I (revenue ratios); Fix 3 — doughnut chart [1] payer rows moved `O14:O19` → `O9:O14` contiguous, series range shrunk `$O$8:$O$19` → `$O$8:$O$14` with rebuilt 7-pt caches. No template asset / no row inserts. Idempotent — the data-move is guarded to the buggy state (`O9` empty AND `O14 == "Medicaid"`), so re-runs and already-fixed Dashboards aren't corrupted. Full chain v0.2.4 → v0.2.9 tested clean. **Resolved the v0.2.8 collision** — BL-0020 is now unambiguously v0.2.9, BL-0022 keeps v0.2.8, and the closed PR #34 branch's `migrate_to_v028.py` is superseded (do NOT revive — use `migrate_to_v029.py`). Bundled file unchanged (stays user-managed v0.2.4 per BL-0021); v0.2.9 only makes the *chain* reproduce correct charts.

### Closed 2026-05-19 (Bundled-Analyzer wholesale reset · BL-0021 + BL-0020)

- ✓ **Bundled `ALF_Financial_Analyzer_Only.xlsx` wholesale-replaced with user's hand-edited Excel copy** (BL-0021, NOT a substrate bump). User opted to replace the v0.2.7-derived bundled file with their OneDrive copy at `C:\One Drive Business\OneDrive - (na)\office\rent_roll_app\ALF_Financial_Analyzer_Only.xlsx`. Bundled file now has `Cover!B8 = "v0.2.4"` and lacks BL-0012 (Section M6), BL-0016 (AH4 fill), BL-0017 (144-cell intentional-blank), and BL-0018 (v0.2.7 Dashboard structural with AZ anchors). User informed of regressions before approving (twice). Migration scripts v0.2.5 → v0.2.8 stay in `tools/migration/` for reproducibility. **The bundled file is now a user-managed artifact** — future substrate work needs to either accept regressions or forward-roll first via the migration chain.
- ✓ **Dashboard `N1` = `"Last updated: 2026-05-19"`** stamp per user request (BL-0021 second deliverable). Static text, Calibri 10pt italic gray `FF595959`, right-aligned. Position: between the title merge (`B2:M2`) and the right-side data table at `O1:Q3`. Static date intentional (intent = "when was the file last edited," not "always show today" via TODAY()).
- ✓ **BL-0020 chart-data-link fixes** present in bundled file via wholesale-copy (PR #34 / `migrate_to_v028.py` closed unmerged but the fixes themselves are in the user's authored Dashboard). `Dashboard!C97:C108` → EGI row 26 (was Housekeeping row 21), `Dashboard!F90:F93` → revenue ratios at col I (was unit counts at col B), doughnut chart series range `$O$8:$O$14` with contiguous data at O9:O13 (was `$O$8:$O$19` with empty slices). These fixes were later ported onto the migration chain proper as `tools/migration/migrate_to_v029.py` (substrate v0.2.9, 2026-05-21 — see the "Closed 2026-05-21" entry above). The original PR #34 branch `claude/bl-0020-dashboard-data-link-fixes` (which held a now-superseded `migrate_to_v028.py` + `v028_assets/`) has been **deleted** — use `migrate_to_v029.py` instead.

### Closed 2026-05-19 (Substrate v0.2.7 — Dashboard sheet redesign · BL-0018)

- ✓ **`Investment Dashboard` sheet replaced with redesigned `Dashboard` sheet.** Sheet count stays at 15. User authored the new sheet externally in Excel and dropped it in on 2026-05-19. New sheet: 437 cells, **6 native Excel charts** (BarChart × 2 + DoughnutChart + 3 more titled charts), 72 merged ranges, 17-col visible layout (B:Q), navy tab color `FF1F4E79`, AZ1:AZ5 anchor block. Pure formula-reference layer — 96 unique cross-sheet refs (Cover B5, T12 Analytics × 52, Rent Roll Recon × 31, Monthly Trending × 12); 95 resolve to populated cells on v0.2.6, the one outlier is `Cover!B5` (Property Name, user-populated at runtime via `Property_Name` named range). Migration via `tools/migration/migrate_to_v027.py` — sources from committed template asset `tools/migration/v027_assets/dashboard_template.xlsx` (26 KB single-sheet workbook). Cells copied via the established `_copy_cell` helper; charts copied via `copy.deepcopy(chart)` since openpyxl Chart objects carry their series references as string formulas that survive deep-copy. Anchor list re-roll: 15-sheet list updated `"Investment Dashboard"` → `"Dashboard"`. 14-check verify, idempotent (gate checks Cover!B8 == v0.2.7 AND Dashboard at index 1 AND Investment Dashboard absent). **Drift not carried forward:** the user's authored file was based on v0.2.4 and had been round-tripped through Google Sheets / LibreOffice (re-introducing `_xludf.MINIFS` / lowercase `minifs` prefixes on RR_Calc + Rent Roll Recon, missing the v0.2.5 Section M6 rows, missing the v0.2.6 BL-0016 AH4 fill, missing the v0.2.6 BL-0017 144-cell intentional-blank styling, plus an accidental T12 Analytics anchor relocation AZ→AM). Migration starts from the current v0.2.6 base and only adds the Dashboard, preserving all v0.2.5 + v0.2.6 substrate work intact (post-migration spot-checks confirmed). UW-BACKLOG is now empty.

### Closed 2026-05-16 (Substrate v0.2.4 — Investment Dashboard sheet)

- ✓ **Investment Dashboard sheet added at workbook front.** New `Investment Dashboard` sheet inserted at index 1, immediately after `Cover` (sheet count 14 → 15). 97 rows × 7 cols (B2:H98), 335 styled cells, sourced from the Beaufort populated Analyzer (Rent Roll 1.31.26 + T-12 1.31.26). Pure formula-reference layer over `T12 Analytics` + `Rent Roll Recon` — no existing-data mutation, no row inserts, no named-range additions, no formula on any other sheet changes. Seven sections: (1) AT-A-GLANCE headline tiles (rows 7-9: Beds / Occupancy / EGI / EBITDARM margin / Going-In Cap / Price-per-Bed), (2) OCCUPANCY & CAPACITY (rows 11-17), (3) REVENUE & RATE PERFORMANCE (rows 19-28), (4) MARGIN & COST STRUCTURE (rows 30-46), (5) VALUATION & ACQUISITION (rows 48-57), (6) PAYER MIX (rows 59-68), (7) AL CARE LEVEL DISTRIBUTION (rows 70-81), plus KEY RISKS & NORMALIZATION CALLOUTS (rows 85-94, 🔴🟠🟢 flagged). Migration via `tools/migration/migrate_to_v024.py` — copies sheet from a committed template asset at `tools/migration/v024_assets/investment_dashboard_template.xlsx` (335 cells with full style preservation), 11-check verify, idempotent (gate checks both version stamp AND sheet-exists-at-position-1). Substrate version stamp v0.2.3 → v0.2.4 across `Cover!B8` and all 15 anchor `AZ4` cells (anchor list grows 14 → 15 because Investment Dashboard joins). All but one of the 56 distinct dashboard cell refs into `T12 Analytics` resolve to populated cells (`T12 Analytics!E117` Purchase Price is an analyst-input cell, expected blank). Cross-track work — user-authorized 2026-05-16 (chat was originally a Track 1 password-gate session for the Streamlit app; user explicitly pivoted).

### Closed 2026-05-14 (Substrate v0.2.3 — Rent Roll Recon row 16 GPR fix · BL-0015)

- ✓ **Rent Roll Recon row 16 — "RR gross contracted base rent / mo" mis-calculating against its own H-note intent.** User-reported on 2026-05-12 against the populated Homestead v0.1.10 Analyzer: row 16 read $565,140 but the Rent Roll Input Market Rate total was $809,567 — and the row's H-column note ("Gross contracted rates before concessions") clearly described the latter. Originally implemented as substrate v0.1.11 in PR #12; PR went stale while main moved through v0.1.12 → v0.2.2 (and v0.1.11 substrate number was reused on main for an unrelated chart-axis fix). Re-implemented here as v0.2.3 against the current 14-sheet anchor list. Fix: B16/C16/D16 rewritten to sum `'Rent Roll Input'!$G` over all units (no status filter) by care type, A16 label rewritten to "RR Gross Potential Rent / mo  (Market × all units)", H16 note rewritten to state GPR semantics + identify row16-vs-row17 gap = vacancy + market-vs-actual premium. Rows 17-20 unchanged. Migration via `migrate_to_v023.py` (3 ops, 9-check verify, idempotent). Closes UW-BACKLOG BL-0015.

### Closed 2026-05-11 (RR v1.16.0 + Substrate v0.1.10 — Data-capture expansion)

- ✓ **RR per-resident charges + housing-revenue fields previously dropped now captured.** User reported (against RR v1.15.0) that the Homestead populated Analyzer was missing 2nd Person rent, Pet, H/K, Laundry, Misc., and other per-resident charges. v1.15.1 (keyword widening — `looks_care` heuristic now catches `pet`, `housekeeping`, `h/k`, `laundry`, `misc`, `diabet`) recovered 13 IL residents' Other LOC $. v1.16.0 added 7 new dedicated columns: `2nd Person Rent $` (4 couples populated $650-$800 in Homestead fixture), `Move-out Date`, `Balance`, `Notes` (33 rows captured incl. "HK $100 eff 3/1- sec occ $650"), `Market PSF`, `Actual PSF`, `ACH`. Companion substrate v0.1.10 adds matching headers at Rent Roll Input!V4:AB4 and extends Total Monthly Rev formula to include +V (2nd Person Rent). 2P rent now reconciles 1:1 against the T12 substrate's pre-existing `2nd Person Revenue` Label. End-to-end verified: Sandra & Darryl Owens (Homestead A14) splits source's $750 ancillary total correctly into V=$650 (SP) + O=$100 (H/K). Cross-track work (Track 1 normalizer / writer / app + Track 3 substrate migration) — user-authorized on 2026-05-11.

### Closed 2026-05-11 (Substrate v0.1.9 — Period-default bug fix)

- ✓ **Rent Roll Recon!B2 period dropdown empty / no latest-date default in Excel** — root cause: pre-existing `_xludf.minifs(...)` formulas in RR_Calc!A2:A13 (Google Sheets / LibreOffice UDF prefix that Excel doesn't recognize → `#NAME?` → IFERROR-empty). v0.1.9 migration drops the `_xludf.` prefix from all 12 RR_Calc cells (native MINIFS works) AND rewrites Rent Roll Recon!B2 to read directly from `Rent Roll Input!$S$7:$S$606` via MAX, so the latest-date default works even if RR_Calc ever drifts again. Migration via `migrate_to_v019.py` (3 ops, 6-check verify, idempotent).

### Closed 2026-05-11 (Substrate v0.1.8 — Branch 3 analytical coverage)

- ✓ **Branch 3 — Analytical coverage shipped (substrate v0.1.8).** New: T12 Analytics property-name & period-end auto-fill (B2 3-priority RR→T12→Cover, E2 LOOKUP rightmost-populated month), 5 chart objects on T12 Analytics K1:V44 (occupancy stacked col / rate-band histogram / payer-mix doughnut / 12-mo revenue trend / AL acuity doughnut), 5 conditional formula-driven note cells, Rent Roll Recon B2 latest-date default formula + dropdown DV, section K (IL unit-type mix + sqft + rate dispersion at rows 86-100), section L (MC auto-detect flat/tiered/FFS at rows 102-117). Migration via `migrate_to_v018.py` — 10 ops, 17-check verify, idempotent. Substrate cells A3 (RR Input) and A10 (T12 Input) reserved as property-name value targets (refinement 2026-05-11).
- ✓ **Track 1 — RR writer auto-stamp `Rent Roll Input!A3`** (RR v1.15.0). `analyzer_rr_writer.populate_t12()` now accepts `source_filename` kwarg and writes the derived property name. Closes the Track 1 carry-forward opened by substrate v0.1.8. New shared module `property_name.py` with `derive_property_name(filename)` strips date stamps + report boilerplate. Verified end-to-end on Salem / Briar Glen / Homestead filename patterns.
- ✓ **Track 2 — T12 writer auto-stamp `T12 Input!A10`** (T12 v0.2.1). `t12_normalizer_writer.populate_t12_input()` extends its existing `source_filename` parameter to also drive a property-name stamp at A10, via the shared `property_name.derive_property_name()` helper introduced in v1.15.0. Closes the Track 2 carry-forward opened by substrate v0.1.8. Combined smoke test confirms end-to-end pipeline: RR + T12 with same property → A3 + A10 populated; different uploads → each writer stamps its own derived name; T12 Analytics B2 (3-priority RR → T12 → Cover) resolves correctly.

### Closed 2026-05-08 (RR v1.14.0 — Homestead-style format)

- ✓ **Homestead Village Pensacola broker-condensed RR support** — shipped. Source headers `Unit ID` / `Cottage` / `Area` / `Category` / `BR/BA` / `Market / Mo YYYY` / `Actual / Mo YYYY` / `Status` now classify into the right canonical fields. Self-contained vacant rows (no resident name, but `Status=VACANT`) emit instead of being silently dropped — recovered 40 of 176 units that the prior parser was losing. Pre-cleaner cuts the end-of-sheet pricing-summary table at the `Avg Area` second-table header. `Occ w/ NTV` resolves to `Notice` (NTV rule ordered before `\boccupied\b`). Verified end-to-end: 176 rows out, IL=62/AL=62/MC=52 exact match, 0 unmapped.

### Closed 2026-05-08 (T12 v0.2.0 + Substrate v0.1.7, [PR #1](https://github.com/ErikJ-Stack/rent-roll-normalizer/pull/1))

- ✓ **`BrokerFinancialSummaryFormat` parser class** — shipped. Third T12 format detects via `Historical Performance` at A4, walks row 4 for the rightmost contiguous monthly run, applies banner-prefix disambiguation (`Direct Care | Payroll - Wages`) with subtotal-pop, and stops at `Non-Operating` / `Wages Analysis` / `Payroll Summary` banners. Reconciles Homestead to NOI $1,411,323.58 to the penny.
- ✓ **Cluster B — sign-convention guards + partial-year T12 handling** — shipped. `_check_sign_convention()` flags positive-sum CONCESSION rows (suffix-aware so banner names aren't false-positives). `_count_populated_months()` + optional `annualize_partial_year` flag. App surfaces a partial-year warning + sidebar checkbox.
- ✓ **`T12 Analytics!R102` lease formula** — fixed in substrate v0.1.7 with INDEX/MATCH against `T12 Raw Data!R:R`. UW Output R61 Lease will now display real values when source has lease data.
- ✓ **`T12 Raw Data` SUMIFS N501 vs N500 cosmetic** — swept (636 cells) in v0.1.7 migration.

---

## Conventions worth knowing

- **Versioning:** RR app version (`v1.X.Y`), T12 code version (`v0.X.Y`), Analyzer substrate version (`v0.1.N`). Three independent counters. Substrate version is stamped on `Cover!B8` and on every sheet's `AZ4` anchor cell.
- **Newest-at-top** in journal.md, CHANGELOG-RR.md, CHANGELOG-T12.md.
- **Migration scripts are idempotent** — re-running on an already-migrated workbook is a no-op. Pattern: `migrate_to_v01N.py` lives in `tools/migration/`. Always include a verification block at the end.
- **Per-sheet anchor cells** at `AZ1:AZ5` on every sheet — purpose / category / visibility / version / notes. Drives the Workbook Health Map section. Predictable location, verified empty across all sheets pre-v0.1.6.
- **Named ranges** are listed in OPTIMIZATION-DECISIONS.md (Named-range definitions section). Currently: `DescMap_Description`, `DescMap_Label`, `RR_Period_Date`, `T12_Period_Date`, `RR_Input_Data`, `T12_Input_Data`, `Property_Name`.
- **Workbook Health is hidden by default.** Right-click any tab → Unhide → Workbook Health to reach diagnostics, validation, and the workbook map.
- **`Sample Files/` is the local-only T12 fixtures directory** at repo root. Gitignored — files contain real property financials and must not be committed. The four canonical fixtures (Salem / Briar Glen / Homestead Pensacola Financial Summary / Homestead - March 2026 T12) live here and are referenced by `tools/verify_t12_v020.py`. New developers need to populate the directory before running the verify harness.

---

## Six openpyxl quirks that bite migrations

Documented from real bugs hit during migration script work:

1. `wb.defined_names[name] = DefinedName(...)` is the v3.x assignment form. `defined_names.append()` was removed.
2. Empty-string cell values render as `0` in Excel/Calc when read back. Leave the cell truly unset, or wrap with `=IF(ref="","",ref)` in formula context.
3. `Cell.alignment` is read-only. To mutate one attribute (e.g. indent), re-assign the whole `Alignment(...)` object preserving the others. Same for `Font`, `PatternFill`, `Border`.
4. (From 2026-05-06, expanded 2026-05-15 after BL-0001) `insert_rows()` shifts cells but not formula text — full-workbook regex sweep needed to update shifted refs. Lookbehind regex must include colons to catch range endpoints (`F15:Q15`). `insert_rows()` doesn't shift merged-cell range definitions; use `mr.shift(row_shift=delta)` to mutate bounds in-place — `unmerge_cells()` wipes displaced cell content.

   **The qualified-range-endpoint trap (BL-0001 / `migrate_to_v021.py`).** When a formula contains a cross-sheet qualified range like `T12_Calc!$N$1:$N$500`, the qualified-pattern regex matches the *first* cell (`$N$1`) as a single cross-sheet ref — the *endpoint* (`$N$500`) falls outside that match and is then re-caught by the unqualified-ref regex, which assumes it's a same-sheet reference and bumps it on row inserts. Result: the endpoint shifts (e.g. `$N$500` → `$N$505` after a 5-row insert) while everything else in the qualified range stays put. Surfaces as off-by-N SUMIF/SUMIFS drift after migrations.

   **Canonical fix:** capture template formulas you intend to *replicate* (e.g. for new rows) **AFTER** the shift sweep, not before. Reading post-shift bakes in any drift on the template row's own range endpoints, so every replicated row's endpoints stay consistent with each other (even if they're all "wrong" relative to the table size — but consistent matters more than absolute, and the v0.1.7 sweep proved harmless when endpoints are one row past the data). See `tools/migration/migrate_to_v021.py` `step_t12_raw_data()` lines 312-321 for the worked example. The v0.1.6 / v0.1.7 "SUMIFS N501 vs N500 cosmetic" was the first symptom of this same artifact.

5. (From 2026-05-24, after the v0.2.10/v0.2.11 sheet2.xml repair fix) **Label strings that start with `"="` are silently misclassified as formulas.** openpyxl's `Cell.value` setter routes any `str` whose first character is `=` into `data_type='f'` and writes it into the `<f>` element on save. Excel then tries to parse the body as a formula — best case it renders `#NAME?`, worst case it strips the cell on open with the dreaded "Removed Records: Formula from /xl/worksheets/sheetN.xml part" repair dialog. This bit `Dashboard!K13` (v0.2.11 footnote `"= T12 bad debt − annualized AR write-offs"`) and `AR & Collections!B47` (v0.2.10 label `"= Implied closing AR"`) — both intended as plain labels with a leading `=` for typographic effect.

   **Canonical fix:** **never start a label string with `=`.** The "this row/tile equals the formula to its right/below" relationship is structurally obvious from the surrounding layout; the `=` prefix is implied. If you absolutely need the visible `=` glyph, use a non-`=` look-alike (`≈`, `≡`, `:`) or prefix the literal text with a non-breaking space (`" = ..."`) so `startswith("=")` returns False. Either way, add an inline comment so the next reader doesn't "fix" it back. Detection: a whole-workbook scan that flags any `data_type=='f'` cell whose formula body parses as a plain English phrase (no parens, no operators) catches this class — see the diagnostic snippet under commit `24dbafe`.

6. (From 2026-05-26, after the v0.5.0 attempted-then-rolled-back UWT template patch) **`wb.save()` silently drops `xl/metadata.xml` and `xl/webextensions/` on save.** These are xlsx zip parts openpyxl doesn't model in its Worksheet / Workbook object graph, so a load+save round-trip writes them out of existence even though every Cell / merged range / defined name / formula object inspects identically pre vs post. Critical when:

   - **`xl/metadata.xml`** holds the `XLDAPR` (`fDynamic="1"`) properties block for Excel 365 dynamic-array formulas. Any sheet that uses `SORT` / `UNIQUE` / `FILTER` / `ANCHORARRAY` / `XLOOKUP` with a spilled return loses its dynamic-array semantics — Excel demotes the spill to a single cell or renders `#SPILL!` even though the formula text is preserved verbatim (including `_xlfn._xlws.SORT` prefixes). This bit the v0.5.0 attempt against `ALF_UW_Template_v5.xlsx` and would have broken the v0.4.3 Section R/S patch (`Z173 SORT(UNIQUE(FILTER(...)))` + `C173 COUNTIFS(...,ANCHORARRAY(Z173))`) had it shipped.
   - **`xl/webextensions/*`** holds Office Add-in taskpane registrations (e.g. the Claude-for-Excel add-in `wa200009404`). Re-installable but disruptive.
   - **`xl/sharedStrings.xml`** is dropped but openpyxl inlines strings into each sheet on save — functionally equivalent.
   - **`xl/calcChain.xml`** is dropped — Excel rebuilds on open.

   **Canonical detection:** before declaring an openpyxl round-trip safe, diff the xlsx zip part inventory pre/post — not just the Worksheet object graph. Snippet:

   ```python
   import zipfile
   for label, p in [("pre", pre_path), ("post", post_path)]:
       with zipfile.ZipFile(p) as z:
           print(f"{label}: {len(z.namelist())} parts")
           for n in sorted(z.namelist()):
               print(f"  {z.getinfo(n).file_size:>10,}  {n}")
   ```

   **Canonical fix:** if the workbook uses dynamic-array formulas or Office Add-ins, **don't round-trip through openpyxl**. Either edit via Excel directly (operator-authored, or scripted via `win32com` on a Windows machine with Excel installed), or use a lower-level xlsx library that preserves arbitrary parts (`python-xlsx`, or direct `zipfile` + `lxml` manipulation of parts openpyxl doesn't model). For Track 4 specifically, this crystallizes the protocol-path argument: the operator authors template changes in Excel via Cowork; Claude Code owns registry + writer, not the template file.

   **Update (UWT v0.6.1, 2026-05-28) — the UW Template writer now auto-repairs this.** Because the writer *must* round-trip the template through openpyxl (it writes the populated data), `uw_template_writer._restore_dynamic_arrays(output_bytes, template_bytes)` runs after `wb.save()` and re-injects `xl/metadata.xml` + the per-cell `cm` markers via pure `zipfile`+`re` (no lxml). This is the "direct zipfile manipulation" path above, made routine: the populated UW Template output now ships with working Section R/S dynamic-array spills, no manual Excel re-entry. The repair is faithful because the writer never edits the dynamic-array anchor cells — it restores the template's original `cm` set to the writer-untouched cells. (This auto-repair applies to the *output* of the populate flow; it does NOT make it safe to round-trip the *blank template itself* through openpyxl for authoring — structural template edits still go through Excel/Cowork.)

---

## Starting a new chat — checklist for Claude

1. Read this file first.
2. Read the top entry of `journal.md` for the most recent session's carry-forwards.
3. Identify which Track the user's request belongs to. If ambiguous, ask before proceeding.
4. If the work is on Track 2 (substrate), assume migration script + spec update + changelog entry are all required deliverables — not just the code.
5. If context starts thinning, recommend spinning up a working MD (e.g. `OPTIMIZATION-DECISIONS.md` style) before the chat hits the wall.

### Verify canonical state before claiming repo state

**The repo lives inside an actively-syncing OneDrive folder.** `ls`, file mtimes, and even Python `json.load()` results from earlier in the same session can serve **stale views** while OneDrive is mid-sync (this bit the 2026-05-26 afternoon Track 4 session — see journal). Before making claims about "what's there" / "what exists" / "what version is on disk," **re-read the canonical source at the decision point**, not at session start:

- **For tracked files** — `git ls-files <path>` is authoritative for "is this tracked." `git log --oneline -5 -- <path>` shows when it last changed. Trust git, not `ls -la`.
- **For working-tree state** — re-run `git status -s` at decision points. Don't trust mental models of state built up from earlier turn outputs.
- **For data files (registry, config, etc.)** — re-`cat` / re-`Read` the file just before drafting work that depends on its contents. OneDrive can serve a stale read on the first try and the canonical value on a later try, within the same session.
- **For xlsx round-trips** — the cell-level Worksheet-object view is **necessary but not sufficient** for fidelity. Always diff the zip-part inventory pre/post too — see openpyxl quirk #6.

### Track 4 preflight

Before drafting any handoff brief, registry edit, or claim about Track 4 state, run:

```bash
git log --oneline -5 && git status -s && \
python3 -c "import json; r = json.load(open('tools/uw_template/registry.json')); print(f'registry_version: {r[\"registry_version\"]} | concepts: {len(r[\"concepts\"])} | open_questions: {len(r[\"open_questions\"])}')" && \
ls tools/uw_template/handoffs/ && \
grep '^UWT_VERSION' app.py
```

Catches the four-mistake class from 2026-05-26 afternoon: stale registry read, missing-from-`ls` handoff files, version-constant drift, forgotten in-flight handoffs.
